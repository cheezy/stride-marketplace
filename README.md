# Stride Marketplace

Plugins for [Claude Code](https://docs.claude.com/en/docs/claude-code) — task lifecycle, AI-powered security review, structured ideation, and a file-only lightweight companion for the [Stride](https://www.stridelikeaboss.com) task management platform, plus [`stride-exploratory-testing`](#stride-exploratory-testing) for charter-based exploratory testing sessions and a LaunchDarkly feature-flag codegen plugin. Each plugin is independent and can be installed on its own.

## Installation

Add the marketplace to Claude Code once:

```bash
/plugin marketplace add cheezy/stride-marketplace
```

Then install any subset of the plugins below.

## Available Plugins

| Plugin | Version | One-line summary |
|---|---|---|
| [`stride`](#stride) | 1.68.0 | Task lifecycle for Stride kanban — claim, complete, create, decompose, with automatic Claude Code hook execution, per-file diff capture (G148/W719 contract), the `## after_goal` hook section, the five review_queue-scored fields (`acceptance_criteria`, `testing_strategy`, `security_considerations`, `pitfalls`, `patterns_to_follow`) called out as first-class deliverables across the task-authoring skills, (v1.18.0+) project-level checks in `task-reviewer` — reads `CODE-REVIEW.md` at the project root and emits a `project_checks[]` field in the structured reviewer payload, (v1.19.0+) per-section `testing_strategy`/`patterns`/`pitfalls` verdict objects, completion now persisting the reviewer's full structured `reviewer_result` block (not the thin issues-found envelope), and an `after_doing` hook fix that resolves the `/changed_files` upload creds from `.stride_auth.md` so diffs upload even when the completion curl uses `$STRIDE_API_URL`/`$STRIDE_API_TOKEN` shell variables, (v1.20.0+) two new slash commands `/stride:create-tasks` and `/stride:create-goals` with a `--dir`/`--context` markdown-context option that route through `stride-workflow`, and (v1.21.0+) `security_considerations` elevated to a first-class review_queue-scored field with a `task-reviewer` **Security Considerations Alignment** step and a `security_considerations` section verdict (reviewer schema bumped to `1.3`), (v1.22.1+) a fix so completion persists `reviewer_result` **verbatim** (passthrough, never re-enumerate) — restoring the dropped `project_checks` the review queue's Code review panel renders, and (v1.23.0+) the `project_checks[]` per-entry `status` enum gains `not_applicable` with the reviewer now emitting every `CODE-REVIEW.md` bullet (inapplicable ones marked N/A rather than omitted) so the review-queue Code review panel shows the full checklist (reviewer schema bumped to `1.4`), and (v1.24.0+) review reports must be delivered **complete — NO EXCEPTIONS** (G222): every supplied review field is passed to the reviewer and the reviewer's whole structured output (every project check and section verdict) reaches the server intact, via a count-checked whole-object `reviewer_result` passthrough and a mandatory pre-submission self-check hard gate — fixing the recurring `not_assessed` security considerations and truncated `project_checks` (3 of 26), and (v1.25.0+) the changed-files diff upload now survives the after_doing timeout — early capture and PUT before the quality-gate commands run (post-commands refresh kept), a `.stride-diff-upload-state`-backed before_review self-heal retry on missing/stale/non-2xx state, PowerShell parity (plus a repair of the ps1 test suite that failed at baseline on pwsh 7.6), and 120s → 300s Bash hook timeouts with the budget documented, and (v1.28.0+) the claim-time `TASK_BASE_REF` is always refreshed — a persisted-output file fallback plus an unconditional base-ref refresh so an oversized claim response can no longer leave a stale ref that makes `changed_files` span unrelated commits (G224), and (v1.29.0+) the optional free-form `technical_details` task field is documented across the creation, enrichment, decomposition, and workflow/exploration guidance — not a review_queue-scored field (G243), and (v1.30.0+) the creation skills now document the optional `created_by_agent` field on the create request bodies so the `/agents` feed attributes the creating agent instead of a `?` — set to the plugin's own agent name (same as `agent_name` on claim/complete), create-only and forbidden on `PATCH`, propagated from a batch goal to its child tasks (G254), and (v1.30.1) internal submission-readiness doc housekeeping — no runtime change, and (v1.31.0+) the `create-tasks`/`create-goals` commands gain an explicit terminal state — after creating, the `stride-workflow` orchestrator reports the new identifiers, clears the activation marker, and stops instead of auto-claiming/building the just-created task — plus a Backlog claim-fail guard so a failed claim never falls back to building outside the lifecycle (G284), and (v1.32.0+) an **API Notes & Limitations** section in the `stride-workflow` skill documenting that tasks cannot be reparented (`parent_id` is creation-only) with no DELETE endpoint — moving/removing is a human board-UI action — and that raw HTTP calls need a curl/browser-like User-Agent to avoid the hosted edge's `403` `error code: 1010` (G286), and (v1.33.0+) hook executor hardening — per-hook timeouts enforced per the documented budgets with process-group kills and exit-124 reporting, server-supplied hook env forwarded so `## after_goal` receives the `GOAL_*` vars, real `duration_ms` telemetry in the hook stdout JSON, quote-aware backslash line continuation in the `.stride.md` parser (the docs' multi-line `gh pr create` example now runs as one command), a claim-time dirty-baseline guard so pre-existing unrelated edits never pollute `changed_files` with the auth file hard-excluded from every upload, plus a documentation accuracy sweep (stale five-field completion claims, `hook-diagnostician` after_goal coverage, contiguous step numbering, capture docs matching the executor) — port parity for the five port plugins is follow-up work (G287), and (v1.34.0+) the `## after_goal` hook now fires **reliably regardless of `/complete` response size** — a harness-truncated `tool_response.stdout` no longer breaks detection because the hook prefers a canonical full-response file (`.stride/.last-api-response.json`) and, when that is absent/truncated, spawns a fresh `GET /api/tasks/:id/after_goal_status` call (not subject to Bash-tool truncation, zero agent cooperation) as the authoritative guarantee — mirrored in the PowerShell hook, with end-to-end truncation tests (388 bash / 270 pwsh) and a clean no-op against servers without the endpoint (G313), and (v1.35.0+) the `changed_files` diff upload now targets the task id parsed from the `/complete`|`/mark_reviewed` command URL rather than the stale claim-time env-cache `TASK_ID`, so a corrupt or stale cache can no longer upload the diff to the previous task (D127), and it fails loud on a terminal upload failure — the `before_review` self-heal (the last retry) logs a distinct `CHANGED_FILES UPLOAD UNRESOLVED` message and appends `unresolved=yes` to `.stride-diff-upload-state`, fail-soft and self-clearing on the next successful PUT (W1658); both mirrored in `stride-hook.sh` and `stride-hook.ps1` with tests (D126, W1661, D127, W1658), and (v1.36.0+) the review diff can no longer show another clone's work or silently drop committed files — `TASK_BASE_REF` is captured **after** `## before_doing` runs (the section's `git pull` moves HEAD; a pre-pull base spanned another machine's pushed commits, D132), any inherited base is stripped at claim interception and rewritten post-section with a `TASK_BASE_REF_TRUSTED` marker, every capture passes through a `resolve_snapshot_base` trust guard (loud stderr recompute from the task branch point for unresolvable, non-ancestor, or unmarked pre-branch-point bases; resolved once per task window and persisted for the before_review self-heal so push-in-`after_doing` and push-before-complete workflows stay safe), and paths committed between base and HEAD always survive the dirty-baseline filter (D137: committed task files whose content matched claim-time hashes vanished from the snapshot) — bash + ps1 parity with two-clone regression fixtures (D142), and (v1.37.0+) every documented create payload now carries a top-level `agent_name` — beside the `task` root key for `POST /api/tasks` and beside the `goals` root key for `POST /api/tasks/batch`, set to the same plain agent name sent as `agent_name` on claim/complete — the always-sent fallback the server reads when the create-only, un-backfillable `created_by_agent` is omitted (which left tasks permanently attributed to a `?` avatar), documented with the full five-step resolution order and an explicit display-metadata-only (never an authorization signal) caveat; the same release fixes `stride-creating-tasks` documenting the single-create body **without** its `task` root key (the server 422s on a bare task object — a new Request Envelope section shows `{"agent_name": ..., "task": {...}}`) and four unquoted JSON keys in its examples that made the most-copied block unparseable (W1684), and (v1.38.0+) an optional, gracefully-degrading integration with the `stride-exploratory-testing` plugin — the `stride-workflow` orchestrator gains a gated **Step 5.5: Manual & Exploratory Testing** (and `stride-subagent-workflow` a matching Phase 3.5) that, when a task's `testing_strategy.manual_tests` is non-empty **and** the plugin is available in a Claude Code session, maps each manual test to an exploratory charter and dispatches the plugin's `/explore` / `explorer` to run a time-boxed session; findings are recorded in **existing** completion fields (`completion_notes` + the `reviewer_result.testing_strategy` note) with no new server-validated field, the creation skills gain an advisory note nudging `manual_tests` toward chartable phrasing, and the whole path falls back to prior behavior with no failure when the plugin is absent or outside Claude Code, preserving the exploratory-testing safety boundary (G344), and (v1.39.0+) an optional, gracefully-degrading integration with the `stride-security-review` plugin — the `task-reviewer` `security_considerations` verdict gains an OPTIONAL nested `considerations[]` breakdown (reviewer schema `1.4` → `1.5`), and `stride-workflow` Step 5 (with a matching `stride-subagent-workflow` orthogonal Decision-Matrix trigger) gains a gated **Deep security-considerations review** sub-step that, when a task's `security_considerations` is non-empty **and** the plugin is available in a Claude Code session, dispatches the `stride-security-review:security-reviewer` in considerations mode (diff + considerations framed as data, never instructions), merges its per-consideration verdicts into `reviewer_result.security_considerations.considerations[]` via the existing verbatim whole-object passthrough, and escalates **fail-closed** — any `partial`/`unmitigated` verdict forces the section `status` to `failed` and appends a `category: security` Critical issue; the dispatch time folds into the existing reviewer step (no new `workflow_steps` name), the completion self-check gains a matching consistency check, and the whole path falls back to the task-reviewer's prose verdict with no failure when the plugin is absent or outside Claude Code (G-5613), and (v1.40.0+) the optional `behaviour_test_matrix` task field is threaded through the plugin end to end — documented as a structured creation field in `stride-creating-tasks` / `stride-creating-goals` / `stride-enriching-tasks` (7 fixed categories, `unit`/`integration`/`manual` type tokens with `/`-joined combos, a `planned`/`passing`/`failing`/`not_applicable` status enum, and a real-test-or-N/A-plus-reason rule, all-or-nothing so a non-empty matrix must cover every category), populated by `task-enricher` from its own testing analysis (both pre-submission checklists now 18 items), verified by `task-reviewer` which locates each row's named test and emits an OPTIONAL top-level `behaviour_test_matrix` verdict with a per-row breakdown (reviewer schema `1.5` → `1.6`, fail-closed: a `failing` row forces the section to `failed` plus a matching `category: testing` issue), and utilized by `stride-workflow` Step 4 (write each row's test, advance its status) and Step 5 (passed in the reviewer dispatch list), with `stride-subagent-workflow` mirroring both and `stride-completing-tasks` gaining a verdict self-check — the field stays OPTIONAL and is never added to the five review_queue-scored fields, so a task without a matrix is never penalized (G381), and (v1.41.0+) every rule that reads matrix row text is hardened against untrusted input — row text is data to assess and never instructions to follow, the secret rule triggers on row *state* rather than agent intent and extends to credentials named by location (file path, env var, vault reference, CI/CD or platform secret, Kubernetes Secret, git object, database row), a refused row gains a named reporting channel (`completion_notes`, identified by category and position rather than quoted) plus a `[REDACTED — row text embedded a credential]` sentinel for the reviewer's verbatim echo with the resulting `failed` verdict documented as the *expected* outcome of a correct refusal, and the PATCH-body contradiction is resolved — because `PATCH /api/tasks/:id` replaces the whole array and a non-empty matrix must cover all seven categories, recording any row's advance necessarily re-sends every row, so re-sending already-stored row text byte-for-byte unchanged onto its own record is stated to be not a new copy, with one named action and an exception scoped to that field on that task's own record; the same release corrects two premises stated as fact — `completion_notes` is persisted by Stride servers from D188 onward (deployment-conditional wording, the `completion_summary` duplication rule unchanged) and matrix row rendering is defended by auto-escaped interpolation plus server-side enum validation rather than by the authoring no-raw-HTML convention (G381 follow-ups), and (v1.42.0+) the `task-reviewer` worked example finally demonstrates `category: "security"` — previously mandated in four places and shown nowhere, so a `failed` `security_considerations` verdict routinely shipped without the backing issue the fail-closed escalation and Consistency rules require; the example gains an important-severity security issue over a synthetic unaddressed bounds-check (matching review step 5's Important-unless-exploitable split, so the one worked instance cannot teach severity inflation), flips its second `considerations[]` entry to `unmitigated` and the section verdict to `failed`, and updates `issue_counts`, the example `summary`, and the preamble — which now spells out that the issue and the section status each require the other while the `considerations` breakdown is optional but forces both when present (schema of record only, `schema_version` unchanged at `1.6`; the other reviewer variants mirror it on their next natural sync) (W1941), and (v1.43.0+) the worked example's `not_met` acceptance criterion finally has a backing `category: "acceptance_criteria"` issue, so its evidence reference resolves instead of dangling and another mandated-but-undemonstrated category is shown (`pattern` remains, deferred) — with the preamble resolving the Critical-vs-Important split by documenting that review step 1's three-value scale collapses into the two-value emitted enum (D191), and (v1.44.0+) a real finding now outranks `not_assessed` in all four section tiles and the two nested-array escalation rules bind in both directions, with deliberately opposite reverse cases (D193), and (v1.45.0+) the reviewer worked example stops echoing a matrix row `passing` for a behaviour the same review proves defective (D198), (v1.46.0+) the severity collapse is stated at review step 1 where the decision is made rather than only in example commentary (D199), and (v1.47.0+) the completion gate's bidirectional checkbox is split into a scannable check plus a verbatim sub-block (D200), and (v1.48.0–v1.61.0) a consistency sweep over both optional integrations — severity mapping and Critical escalation, non-interactive dispatch surfaces, session budget, `/harden`, and then the reachability, coverage-honesty and telemetry fixes that make a small 0-1 `key_files` task actually reach Step 5.5 and the deep security sub-step, and (v1.62.0+) the orchestrator skill is 42% smaller — gated and cold content moved into sibling reference files loaded on demand, never deleted — plus a stale Step 7 example that had been causing hard `422`s on dispatched-review completions, and (v1.64.0+) task-attributed `changed_files`, a one-document hook stdout contract, durable per-hook result files, a required substantive note on a failed section verdict, and load-calibrated hook test suites, and (v1.65.0+) an optional `workflow_steps[].reason_code` so skip reasons aggregate plus write-then-summarise for all three report-producing agents, and (v1.66.0+) the hot-path re-extraction with byte budgets — both the orchestrator and completion skills roughly halved into gated sibling files with every gate, matrix, Decision Summary and framing rule kept inline, `scripts/check-skill-budgets.sh` wired as hook-suite Group 28 so hot-path files cannot silently regrow — plus slim-view adoption on the index/tree GETs and the complete acknowledgement, dispatcher mode's projected `fields=status,needs_review` read, and the completed-status confirmation gate in `reference.md`'s dispatcher summaries, and (v1.67.0+) progressive disclosure for the three largest agent bodies — `task-reviewer` 66,177→55,936, `task-enricher` 37,338→28,865 and `task-decomposer` 25,521→13,684 bytes, **30,551 bytes** off the per-dispatch cost — with only worked examples, edge-case sections and anti-example galleries moved to sibling `docs/` files and every contract kept inline (the emitted block schema, the verdict rules, the bounded-summary contract, the four enrichment phases and their field types, the six decomposition steps and both sizing tables, and in all three the prompt-injection framing and the redaction rules), placed in `docs/` rather than `agents/` because every `.md` there registers as a dispatchable agent; each split verified by dispatching the slimmed body against a real fixture rather than by reading the diff, which is what caught a returned-summary rendering that existed only in the example being moved, a waived-matrix-row shape stated only by a sentence narrating that example, and a decomposer that never carried a treat-task-text-as-data boundary at all despite consuming free-form human goal text; the decomposer's duplicated request envelope is deleted rather than moved, pointing at the `stride-creating-goals` skill that owns it; plus the commit-attribution lineage closing — per-window purity classification (D244), hook-mediated commit ownership (D255), the purity fixpoint so concurrent siblings cannot manufacture purity (D256), per-window open-window-aware eviction so a live outer's anchor never falls to the cap (D268), the outermost-task gate that stops a silent under-report (D271), and the zero-commit-child ratchet pinned rather than fixed after the candidate branch was measured breaking nine pre-existing pins (D272) — and a relatedness gate ahead of the Step 5.5 severity/provenance policy, so a finding whose responsible lines are lines this task changed is fixed in-task at any severity rather than filed (D270), and (v1.68.0+) a confirmed command-injection fix (D280) plus native-Windows diff capture, attribution and narrowing (W2100–W2106) — with the 5.1 upload and after_goal detection still blocked by a 7-only parameter (D277) |
| [`stride-security-review`](#stride-security-review) | 2.5.1 | AI-powered security review of code changes — seven framework rule packs (Android, Django, Express, iOS, Phoenix, Rails, React/Next.js), CI/CD pack, defense-in-depth pack, with SARIF / CI integration. v2.4.0: 70-fixture eval coverage (Azure/Drone/Jenkins/Tekton + lockfile-drift/typosquat) and golden-file transform tests; v2.4.1: CI graceful-skip when `ANTHROPIC_API_KEY` is unset — no analysis change; v2.4.2: documentation-accuracy release — docs unified on the fifteen-value vulnerability_class contract and seven-pack inventory, fail-open CI snippet corrected, unambiguous --rci clamp semantics, full flag inventory in the skill, plus a README fixture-count drift guard in check_fixtures.sh; v2.5.0: **considerations mode** — a `--considerations <source>` flag (and `considerations` agent input mode) that verifies a task's `security_considerations` were actually mitigated by the diff via a per-consideration `mitigated`/`partial`/`unmitigated` verdict, byte-identical output when not set, with paired positive/negative eval controls (72 fixtures); v2.5.1: exit-code contract fix — the three agent-response-failure branches (Step 4a/4b/4.5) now emit the documented `exit 2` on unparseable agent JSON so a `--fail-on` CI gate sees the contracted code |
| [`stride-ideation`](#stride-ideation) | 0.11.0 | Turn a fuzzy idea into a committed requirements doc and Stride tasks via two slash commands. v0.7.0: four-layer resilience model on `/stridify`. v0.8.0: a guided, human-in-control `/ideate` session (per-round completeness recap, "I'm not sure — propose candidates" path, profile recommendation, `--input` brain-dump seed, intra-session draft autosave/resume, reviewer-findings decision) plus a `/stridify` preview-and-approval gate with `--yes` bypass. v0.9.0: directory-submission readiness — `SECURITY.md`, completed manifest metadata, self-contained README, and credential-hygiene + install-path audits (no functional change). v0.10.0: a mandatory, advisory **challenge gate** (assumption-confidence audit, blind-spot scan, two alternatives, cost/risk/complexity/timeline trade-off) after the premortem and before the reviewer pass, folding into a new optional "Design challenge" section. v0.11.0: the decomposer, fixtures, and batch validator align on the **five review-queue scored fields** (validator gains advisory completeness warnings + a hard varchar(255) length check), and `/stridify` stamps `created_by_agent` on shipped goals for `/agents`-feed attribution |
| [`stride-lite`](#stride-lite) | 0.13.0 | File-only lightweight companion — writes Stride-shaped goal and task markdown to disk via `/stride-lite:create-goal` / `/stride-lite:create-task` / `/stride-lite:init`. v0.9.0+ harness-enforced `.stride_lite.md` hook auto-fire (PreToolUse/PostToolUse, cross-platform); v0.10.0+ terminal PENDING → IMPLEMENTED archive move in `stride-lite-workflow`; v0.11.0+ copy installs ship the hook enforcement layer (pre-0.11.0 script installs lacked it — re-run install.sh), harness-executes wording across the init surface, and a 43-assertion smoke suite with hook-routing coverage; v0.12.0+ three optional gated sub-steps dispatching agents from other plugins (exploratory testing, hardening, deep security review), `SECURITY.md`, and four defect fixes including two that had left the native-Windows hook path non-functional |
| [`launchdarkly`](#launchdarkly) | 0.3.0 | Generate correct, testable, and removable LaunchDarkly feature-flag code — Java server SDK plus the TypeScript SDKs (Node server, client-side JS, React) with skills, scaffolding and flag-removal commands, and an anti-pattern review agent. v0.3.0+: client-side skill modernized to the scoped v4 SDKs (`@launchdarkly/js-client-sdk`, `@launchdarkly/react-sdk`) with a labeled legacy v3 section; `/launchdarkly:ld-scaffold` gains browser and react targets (four total); the reviewer agent gains eight-plus new rules; and a dependency-free `scripts/smoke.sh` validation gate ships |
| [`stride-exploratory-testing`](#stride-exploratory-testing) | 0.2.0 | Drive structured, charter-based exploratory testing sessions in Claude Code — the "explored" half of Tested = Checked + Explored. Six skills (`stride-exploratory-testing`, `chartering`, `heuristics`, `oracles`, `bug-advocacy`, `session`), seven commands (`/charter`, `/nightmare-headline`, `/explore`, `/pair`, `/recon`, `/debrief`, `/harden`), and two subagents (`charter-generator`, `explorer`). Plan charters, run timeboxed SBTM sessions against a running app under an absolute safety boundary, judge results with oracles, and capture a session sheet + debrief. (v0.2.0+) adds `bug-advocacy` — Kaner's RIMGEA plus a four-level severity rubric with explicit criteria — `/pair` for human-driven sessions where Claude suggests probes and names the lens, `/harden` to turn confirmed bugs into drafted regression checks in your own detected test framework, and `.exploratory/` session artifacts (debriefs, a charter backlog, a percentage-free coverage map) so findings survive the conversation; the explorer's contract drops the wall-clock fields an agent cannot observe for an agent-native probe budget. Ships worked fixtures and a pure-shell smoke-test harness. Encodes established exploratory-testing practice (Kaner, Hendrickson, Bach, Whittaker) |

---

## stride

Task lifecycle skills for Stride kanban: claiming, completing, creating tasks and goals, enriching tasks, workflow orchestration, subagent dispatch, and automatic hook execution via Claude Code hooks.

**v1.68.0:** a confirmed injection closed, and the native-Windows hook stops being a stub. `stride-hook.sh` loads the env cache with `. "$ENV_CACHE"` under `set -a`, so every value the PowerShell side wrote there was **shell syntax, not data** — three write sites emitted `KEY=value` bare, and a task title of `Fix $(touch /tmp/marker)` was a command substitution bash executed at source time (**D280**, confirmed executing rather than theorised). Every write site now escapes through a `sq_escape` equivalent, the loader unquotes with an exact inverse, values are flattened so a newline cannot manufacture a second cache line, and the shell-steering fence became an **allow-list** of the cache's own namespace after a deny-list was found missing `GIT_SSH_COMMAND` and `GIT_EXTERNAL_DIFF`. Separately, `stride-hook.ps1` had **no capture step at all**: it PUT a snapshot something else was assumed to have written, and on native Windows that something is nobody, so a run produced no snapshot with no error. It now builds the snapshot, carries the five per-task record families, runs the same window-classification engine as bash (D236/D244/D256), evicts **per window** so a live outer task's anchor survives (D268/D274 — measured: 19 concurrently open children left the outer intact, 20 lost both its anchor and its deliverable), and replays the capture-time narrowing verdict on retry rather than re-deriving it (D273). The ps1 suite went 530 → 961 assertions; the bash suite is unchanged at 787 and both bash files are byte-identical across the port. **Not parity yet, and the release says so:** on Windows PowerShell 5.1 `Invoke-WebRequest -SkipHttpErrorCheck` is a 7.0+ parameter sitting at two call sites — the diff cannot upload (recorded `000`, indistinguishable from a refused connection) and after_goal detection is a silent no-op under its `catch { return }` (**D277**) — and nothing in the repo has ever executed the hook under `powershell.exe`, so the new 5.1 gate is static only (**D237**).

**v1.67.0:** smaller agent bodies, and the commit-attribution lineage closes. The three largest agent prompts are re-paid on **every** dispatch, so their reference material moves to sibling files under `docs/` that the agent Reads on demand: `task-reviewer.md` **66,177 → 55,936**, `task-enricher.md` **37,338 → 28,865**, `task-decomposer.md` **25,521 → 13,684** — **30,551 bytes** off the per-dispatch cost. Only illustration moved; every contract stays inline, and the files go in `docs/` rather than `agents/` because every `.md` there registers as a dispatchable agent. Each split was **verified by dispatching the slimmed body against a real fixture** rather than by reading the diff, and that is what caught what a diff review would not have: the reviewer's returned-summary rendering existed only in the example being moved (it is now specified inline for the first time), the enricher's waived-matrix-row shape was stated only by a sentence narrating its removed example, and the decomposer turned out **never** to have carried a treat-task-text-as-data boundary despite consuming free-form human goal text and grepping arbitrary repository content. The decomposer's duplicated request envelope was deleted rather than moved — it restated what `stride-creating-goals` owns, and relocating a duplicate preserves the drift instead of removing it. Alongside, the commit-attribution lineage closes: per-window purity classification (D244), hook-mediated commit ownership (D255), the purity fixpoint (D256), open-window-aware eviction (D268), and the outermost-task gate (D271) — with the remaining zero-commit-child steal **pinned rather than fixed**, because the candidate branch was implemented behind a flag and measured to break nine pre-existing pins and revert per-window attribution for every hand-committing agent (D272). A relatedness gate now sits ahead of the Step 5.5 severity policy: a finding whose responsible lines are lines this task changed is fixed in-task at any severity rather than filed (D270).

**v1.66.0:** the hot path re-extracted, and budgets so it stays that way. **G404's saving had been eaten silently in four days** — the orchestrator skill regrew from 80,523 to 119,880 bytes (roughly 13,700 tokens re-spent on every main-loop request) — so this release reverses the regrowth and gates against a third occurrence: `stride-workflow/SKILL.md` returns to **89,647 bytes** and `stride-completing-tasks/SKILL.md` drops from 100,085 to **56,750 bytes**, each with its cold material moved to gated sibling files reachable from pointers at the original sites, every gate, decision matrix, Decision Summary, and prompt-injection framing rule kept inline, and the completion contract re-verified against the server's own validation modules with no field requirement, schema shape, or enum value changed. `scripts/check-skill-budgets.sh` runs as hook-suite **Group 28** with budgets 12-13% above post-extraction sizes — drift detectors, not targets — and its failure output names the file, size, budget, the extraction pattern, and where the budget table lives. Slim views are adopted where they pay: the index/tree GETs carry `response_view=slim` (the bare index measured 2.4 MB against production), the complete curl adopts the slim acknowledgement, dispatcher mode's capture-and-project workaround becomes a projected `fields=status,needs_review` read, and `reference.md`'s dispatcher summaries gain the completed-status confirmation gate — completed loops only after the projected read agrees, with a disagreement or failed read reported as abandoned. The claim and next curls deliberately stay full.

**v1.65.0:** skip reasons that aggregate, and reports that stay on disk. **`workflow_steps[].reason_code`** is a new optional field beside the free-text `reason`, carrying one of six values — `decision_matrix_skip`, `ran_inline`, `hook_body_empty`, `subsumed_by_task_spec`, `folded_into_prior_step`, `matrix_deviation` — **derived by classifying every skipped entry persisted on a real board** rather than invented; the measured problem was 73 skipped entries producing 58 distinct prose strings averaging 145 characters, which grouped verbatim is very nearly one row per entry. It is optional and omitting it stays valid, so agents that predate it complete unchanged on any runtime; only a code outside the list is rejected, which is what stops a typo opening its own bucket. `matrix_deviation` is reserved for a step the decision matrix actually called for — it is the one code that records non-compliance, and reaching for `decision_matrix_skip` there would launder a deviation into a sanctioned skip. **All three report-producing agents move to write-then-summarise:** `task-reviewer` persists its structured block and its human report under `.stride/` and returns a summary bounded at 24 lines / 2,000 characters, while `task-explorer` and the generic `Plan` dispatch persist their full findings there and return summaries bounded at a deliberately looser 60 lines / 6,000 characters — looser because those summaries are implemented from rather than parsed, so over-trimming forces the file open every time and saves nothing. Measured before changing anything: a review response ran ~27 KB (~17 KB of it the block), explorer reports 15.5–20.7 KB, one plan report 34.8 KB. A dispatch that supplies no path writes nothing and emits inline, so an older orchestrator — which would never look for the file — keeps working exactly as before, and consumers read only the paths they themselves supplied rather than any path named in a returned summary. The reviewer keeps two files rather than one because `review_report` is what a human reads, and the block alone would reduce it to a stub. Schema unchanged; `schema_version` stays `"1.6"` because the carrier moved and no field did.

**v1.64.0:** three executor defects that were each silent, and a contract that could not be read. **A completing task's `changed_files` absorbed its nested tasks' commits** — `capture_changed_files` diffs `base..working-tree`, so everything committed between a claim and a completion was attributed to that task; replaying the recorded sequence gave an outer task `[fileB.txt, fileC.txt, outerA.txt]` when only `outerA.txt` was its own, and dispatcher mode makes that the normal shape. It now attributes **commits**, not ranges — subtracting a child's range would erase the parent's own change to a file both touched — with a per-task completion ref bounding each nested window and a task's own commits expressed as contiguous ranges so the interleaved case works. One case is knowingly left open and pinned by a test rather than hidden: a commit the outer task makes *while* a nested task is in flight is still attributed to the child. **Hook stdout carried two JSON documents** whenever a primary section and `## after_goal` both ran, so a strict parse failed and every harness-facing field — including the `hookSpecificOutput.additionalContext` channel added earlier — was silently dropped; the executor now emits exactly one document, with the single-section shape unchanged so no existing consumer moves. Note that `jq` cannot detect that defect: both `jq .` and `jq -s` accept a concatenated stream, which is how an earlier guard test asserted the broken value and passed. **Hook durations were unreadable** — the only duration the executor emitted was on the exit-0 path, which reaches the transcript rather than the model — so every section that does work now persists its result to `.stride/.hook-result-<hook>.json`, making `after_goal`'s real figure available for its follow-up PATCH; `after_doing` and `before_review` stay `0`, and the release says why rather than implying they were fixed. Also: a `"failed"` section verdict must carry a substantive note, enforced server-side regardless of the validation flag; and both hook test suites calibrate machine load at each end of a run and scale their wall-clock backstops **clamped below each case's un-killed duration** — the clamp is the load-bearing half, since an unclamped scale would grow a bound past its own `sleep 30` and let a timeout that never fired pass.

**v1.62.0:** goal G404 cuts the orchestrator's per-task context footprint via progressive disclosure — `skills/stride-workflow/SKILL.md` goes **139,972 → 80,523 bytes (−42%)**. Step 5.5 (exploratory testing) and Step 5.6 (hardening) keep their gate, Decision Summary and an explicit load instruction inline while their bodies move to `optional-exploratory-testing.md` and `optional-hardening.md`; every non-Claude-Code branch consolidates into `platform-other.md`, loaded once at Platform Detection instead of sitting resident on the Claude Code path; and six cold lookup sections move to `reference.md`. Content was **moved, never deleted** — each extraction was proved lossless by reconstructing the pre-change file from its parts and diffing byte-for-byte against git, including the six non-contiguous cuts, where the six deliberately-dropped blank lines each appear explicitly in the proof. **The most consequential change is not a size reduction.** Step 7's worked `reviewer_result` example had fallen behind the contract it illustrated, listing five keys where the server's `ReviewContract` requires eight structured sections on any dispatched review and `CompletionResultGate` rejects on them **unconditionally, regardless of the grace flag** — so an agent building its payload from that example was getting a hard `422`. It is now a handoff to the canonical `stride-completing-tasks` schema rather than a corrected example, because a rewritten example would be a third copy with nothing keeping it in sync, which is how the original went stale. Step 5's reviewer dispatch also gains a specified diff derivation (it previously left the method to improvisation — most naturally a bare `git diff`, which silently omits staged hunks and untracked files and returns nothing inside a nested subrepo) plus the data-not-instructions framing and redaction rules that were absent from it. Ships `docs/token-baseline.md`, a reproducible per-task token measurement taken from real transcript `usage` records rather than estimated from byte sizes. **Known limitation:** the goal's headline target of under 40KB is not met and is not reachable by extraction — the largest remaining section is Step 5 at ~20.8KB of hot-path procedure, and closing the gap would mean deleting rules. 42% is the honest ceiling for this technique.

**v1.54.0–v1.61.0:** a consistency sweep over both optional integrations, each item found by an exploratory session run against the integration itself and filed as its own defect. **Reachability was the theme.** Step 5.5's gate is `manual_tests` plus plugin availability and the deep security sub-step's is `security_considerations` plus plugin availability — neither has a review precondition, so a small 0-1 `key_files` task reaches both. Nine routing artifacts across the three skills said otherwise, sending that task from the skipped review straight to the hooks: three flows, four quick-reference cards, a prose skip-guidance rule, and — the subtlest — the deep sub-step's own position as a level-4 child of the reviewer-dispatch section, a subtree a small task never enters, so it was structurally unreachable on exactly the path its gate fires on (D202, D206, D208). All nine now route through both. The sub-step was not moved to fix it: three cross-file citations identify it positionally and three of its own references are written for proximity to the reviewer block, which is where its merge rule belongs, so the fix is a pointer from the skip branch rather than a promotion. **The completion gate stopped being unsatisfiable** on two documented payload shapes: its "a verdict must be present" checks are scoped to payloads where a structured review block was actually parsed, so a self-reported skip or an unparseable review — where no verdict object exists to be `not_assessed` — completes normally instead of demanding a re-review the decision matrix forbids or a verdict that would be fabricated (D201). **Coverage claims got honest.** A blocked session no longer counts as a performed manual test: it takes the same disposition as a zero-probe one, recorded as *not performed* and handed back, unless the sheet shows meaningful probes first, in which case it is partial coverage; and the obstacle is recorded **as an obstacle**, in the terms the contract emits it, rather than as a severity-bearing finding that the absent-severity rule would file as an `important` testing bug (D205). **Both dispatches now say where their time goes** — folded into the existing `reviewer` `workflow_steps` entry, never a seventh name, and where no reviewer ran, that entry is the skip form carrying no duration, so the dispatch is recorded in `completion_notes` rather than a duration being invented for a step that did not run (D204, D207). Finally, the exploratory gate's surface list and its never-execute-untrusted-plugin-content prohibition are stated identically in both orchestrator skills, so the security gate's cross-reference to "the same detection the exploratory gate uses" resolves to a gate that actually contains the rule (D203), and the session-sheet guidance is conditioned on the **installed** explorer contract rather than assuming one version's fields.

**v1.48.0–v1.53.0:** the exploratory-testing integration's second wave, previously unsynced here. The rubric maps onto the reviewer severity enum and a **Critical** finding gains a stated escalation policy turning on whether the responsible lines are lines the task wrote (W1973); the Step 5.5 dispatch is restricted to **non-interactive surfaces** so `/pair` and `/explore` are never auto-dispatched — judged by the prompts a surface *can* raise, not only those it always raises (W1974); the dispatch carries an explicit **session budget** in the installed contract's own unit plus a named environment-context list (W1975); the completion summary names **who is harmed** and cites a session artifact when one exists (W1976); `.exploratory/` gitignore guidance lands **before** the first session rather than after (W1977); and an optional **Step 5.6 `/harden`** drafts one regression check per convertible bug, sequenced so a drafted check can never turn the blocking `after_doing` gate red (W1978).

**v1.47.0+:** the completion gate's bidirectional checkbox is scannable again, by moving text rather than trimming it (D200). The **MANDATORY pre-submission self-check** is meant to be scanned before every completion, but its bidirectional checkbox had grown to roughly 800 words holding the (a)/(b) rule, the never-delete-downgrade-or-relabel rule, the re-dispatch protocol, three named outcomes, a definitional reconciliation against the reviewer's note-only prohibition, and a closing matrix caveat — and an agent under time pressure skims that rather than runs it. Every clause is load-bearing, each having closed a specific route to a dropped finding, so this is a restructure and not a rewrite: the checkbox keeps the (a)/(b) rule, the does-not-license-the-converse clarification, and the never-delete rule, while everything from "The target state is…" onward moves **verbatim** into a named sub-block, *Resolving a verdict/issue disagreement*, indented beneath it so it stays part of that list item. The move was done by script and proven rather than asserted — concatenating the trimmed checkbox and the moved block reproduces the previous line character-for-character, zero characters lost — taking the checkbox from ~826 words to ~201. Two sentences are added, one on each side of the seam, both stating the sub-block is part of the check and not commentary, so it cannot read as optional. The six checkboxes are neither renumbered nor merged. The sibling `behaviour_test_matrix` checkbox is long for the same reason and is left alone as out of scope, with its seam recorded for a follow-up.

**v1.46.0+:** the three-value to two-value severity collapse is now stated where the severity decision is made (D199). v1.43.0 documented that review step 1 works on a three-value scale (Met / Partially Met / Not Met) while the emitted `status` enum has two — so a Partially Met criterion collapses to `"not_met"` while keeping `important` severity — but wrote that reconciliation only into the worked-example preamble, ~90 lines after the point of decision. Both normative loci were left unchanged: step 1 still read flatly "If any criterion is Not Met, flag it as a Critical issue", with the working label colliding with the wire value and no qualifier or pointer, and the `acceptance_criteria` hard rule covered only the partially-satisfied case, mentioning neither the wholly-absent case nor `critical` at all. Step 1's bullets now mark the three labels as that step's **working labels, not the emitted enum**, and point at the hard rule; the hard rule gains the wholly-absent case with its `critical` severity. Both loci also now carry the pairing obligation for a `"not_met"` criterion — previously stated only in example commentary, while `project_checks` states its own in both its review step and its schema bullet — mirroring that shape, negative half included.

**v1.45.0+:** the `task-reviewer` worked example no longer echoes a matrix row `passing` for a behaviour the same review proves defective (D198). The Lifecycle / wiring row claimed "The move broadcasts exactly once", named a test asserting it, and echoed `status: "passing"` — while three other legs of the same example state the fictional `move_task/3` broadcasts **twice**, including a `suggested_fix` asking the author to assert single-emission in the very test the row declared passing. Under the file's own Mismatch definition that row was a Mismatch echoed `passing`, in an example prefaced "Mimic this shape exactly". Of the two available fixes this release takes the cheaper one — drop the cardinality from the row rather than flip it to `failing`, which would have required a second `category: "testing"` issue the existing one could not honestly back, an `issue_counts` bump, and rewrites of both the matrix `note` and `testing_strategy.note`. The cardinality is the third acceptance criterion's claim and its failure is already carried there; no count moves. A dangling severity reference is corrected alongside it (`security_considerations.note` said "critical" for an `important` issue).

**v1.44.0+:** a real finding now outranks `not_assessed`, and the nested-array escalation rules bind in both directions (D193). Two rules demanded different values for the same reachable state: a reviewer finding a genuine vulnerability on a task that supplied no `security_considerations` was told by the empty-section rule to emit `not_assessed` and by the Consistency rule to emit `failed`, with no documented winner — and the cheapest escape from an unresolvable constraint is to make the finding go away. The collision was never security-specific; the `testing` instance was already live (a `failing` matrix row on a task with no `testing_strategy`), and `pitfalls`, `patterns` and `testing_strategy` had no carve-out at all. The four-tile verdict rule now states that `not_assessed` means *there was nothing to assess* and so cannot survive a finding actually made: an empty task section plus a genuine matching-category issue is `"failed"`. The rule for a task-supplied section is untouched and the two cases are stated as disjoint, so nothing reopens D60. Suppressing or re-labelling a real finding — dropping it, filing a step-5 security finding as `code_quality`, downgrading it to `minor` to duck the section flip, or confining it to prose — is named as a worse defect than the verdict conflict it avoids. Review step 5's dimension checks now run whether or not the task listed considerations, since otherwise the new rule would be dormant exactly where it is needed. Both nested-array escalation rules gain a reverse direction, and the two are deliberately opposite: a `"failed"` `security_considerations` beside an all-`mitigated` `considerations[]` is **legitimate** — the array breaks down only the task's listed items while the verdict draws on the whole review, so a finding outside the list has no array slot to live in, and forbidding the state would force either suppressing it or dishonestly relabelling a listed item — whereas a `"failed"` `behaviour_test_matrix` beside no `"failing"` row is a **defect**, because `rows[]` enumerates everything that verdict can be about. The completion self-check gains a bidirectional section-verdict/issue gate and matching amendments. Schema of record only — `schema_version` stays **1.6** and neither enum changes.

**v1.43.0+:** the `task-reviewer` worked example's `not_met` acceptance criterion finally has a backing issue (D191). The example's third criterion carried `status: "not_met"` with an `evidence` string ending "…see the critical issue above" — and no such issue existed, so the reference dangled and the example demonstrated the opposite of what two rules in the same file require. `acceptance_criteria` was another category mandated in prose but demonstrated in no worked instance, the same defect class v1.42.0 closed for `security` — though not the last: `pattern` remains undemonstrated and is recorded as deferred rather than silently closed. The example gains an `important`-severity `category: "acceptance_criteria"` entry over the synthetic double broadcast in its own fictional `move_task/3`, `issue_counts.important` goes 3 → 4, the `summary` gains the matching clause, and the criterion's `evidence` now points at that entry. The preamble's two enumerations are reconciled — the criterion had been appended as a sixth item *outside* the five-issue list, making it a peer of the issues rather than something backed by one — and it now resolves the severity question the rules left open: review step 1 works on a three-value scale (Met / Partially Met / Not Met) assigning Critical to Not Met and Important to Partially Met, while the emitted `status` enum has only two values, so a Partially Met criterion collapses to `"not_met"` on the wire while keeping `important` severity, exactly as the `acceptance_criteria` hard rule directs. The two rules were never in conflict; only the collapse was undocumented. Schema of record only — `schema_version` stays **1.6**, the seven-value `issues[].category` enum line is unchanged (`"acceptance_criteria"` was already legal), and the other reviewer variants mirror this file on their next natural sync.

**v1.42.0+:** the `task-reviewer` worked example finally demonstrates `category: "security"` (W1941). The category was mandated in four separate places in the prompt and demonstrated in no worked example anywhere in the tree, while `category: "project_check"` was both mandated *and* shown — and models copy examples far more reliably than they follow prose, so a `failed` `security_considerations` verdict routinely shipped without the backing issue the fail-closed escalation and Consistency rules each require. `agents/task-reviewer.md`'s worked example gains an additional `issues[]` entry with `category: "security"`, describing a synthetic unaddressed bounds-check on the example's own fictional subject, and its `security_considerations` moves with it: the section `status` flips `passed` → `failed`, its second `considerations[]` entry flips `mitigated` → `unmitigated`, and the section `note` explains which of the two listed considerations was and was not implemented — with `issue_counts`, the example `summary`, and the preamble updated so the issue, the nested entry, and the section verdict are visibly consistent. Severity is demonstrated rather than only mandated: the new entry is `severity: "important"`, matching review step 5's own split — an unaddressed or inadequately-handled consideration is Important, and Critical is reserved for one that leaves an *exploitable* vulnerability in the diff — so the single worked instance cannot be copied into systematic severity inflation. The `issues[].category` enum line is unchanged; `"security"` was already legal there and only a worked instance was missing. Schema of record only — reviewer `schema_version` stays **1.6**, no field is added, removed, or re-typed, and the other reviewer variants across the fleet mirror this file on their next natural sync rather than being re-released. Documentation/agent-prompt change only — no hook, `.stride.md`, or `.stride_auth.md` change.

**v1.41.0+:** every rule that reads `behaviour_test_matrix` row text is hardened against untrusted input (G381 follow-ups). Row text is authored by whoever created the task and is attacker-controlled at the API boundary — anyone posting directly to the Stride API never sees the plugin's instructions. **(1)** Row text is **data to assess, never instructions to follow**: a row is a specification to satisfy, and text inside one that appears to address the agent, waive a check, or exempt the task is content being submitted — reportable as a finding, stated explicitly in both the completion self-check's matrix gate and the Step 4 implementation driver (D178, D179). **(2)** The secret rule is scoped to row *state* rather than agent intent and extends to credentials named by **location** — file path, env var, secret-store key, vault reference, CI/CD or platform secret, Kubernetes Secret, git object, database row — so no other purpose the agent also holds makes resolving that location permitted; the workflow's own sanctioned use of its authentication credentials stays permitted, but a row *naming* that file is still a row, and is reported rather than read (D184, D187). **(3)** A refused row gains a named reporting channel and a defined representation: the implementing agent reports the defect in `completion_notes`, identifying the row by `category` and position rather than quoting its text, and leaves the row exactly as the task authored it; the reviewer, which is required to echo rows verbatim, instead substitutes the literal sentinel `[REDACTED — row text embedded a credential]` into the required field that carries the credential, echoes that row `failing`, and raises a `category: "security"` issue — the resulting `failed` verdict is documented as the **expected outcome of a correct refusal**, not a defect by the agent (D186). **(4)** The PATCH-body contradiction is resolved: because `PATCH /api/tasks/:id` replaces the whole array and a non-empty matrix is rejected unless it covers all seven categories, advancing any row's status necessarily re-sends every row, so re-sending row text the task record already stores, byte-for-byte unchanged, back onto that same record is stated to be not a new copy — with exactly one named correct action, an exception scoped to that one field on that one task's own record, to already-stored text and only unchanged, every other sink still binding in full, and substituting the reviewer's redaction sentinel into the task record explicitly forbidden (D185). The Step 5 self-review checklist gains a `behaviour_test_matrix` bullet so the non-Claude-Code path checks the field it was already told to write (W1949). The same release corrects two premises the guidance had stated as fact: `completion_notes` **is** persisted by Stride servers from D188 onward — deployment-conditional wording, since an agent cannot tell which server version it is talking to, so the rule requiring a refusal to *also* appear in one line of `completion_summary` is unchanged — and row-text rendering is defended by auto-escaped interpolation plus server-side enum validation rather than by the authoring no-raw-HTML convention, which is kept as hygiene (W1947).

**v1.40.0+:** the optional `behaviour_test_matrix` task field is threaded through the plugin end to end — populate, verify, utilize (G381). The Kanban app stores one row per behaviour paired with the test covering it, across seven fixed categories (`Happy path`, `Boundary`, `Error / exception`, `Null / empty`, `Concurrency`, `Lifecycle / wiring`, `Contract / serialization`). `stride-creating-tasks` documents the row contract (`category`, `behaviour`, `status` required; `test_name` required unless the row is waived, in which case `na_reason` is; `type` one of `unit`/`integration`/`manual` or a `/`-joined combination) with WRONG/RIGHT examples and a full seven-row sample, mirrored for nested tasks in `stride-creating-goals`. `stride-enriching-tasks` and `agents/task-enricher` build it in the testing-analysis step by projecting the derived unit/integration/manual/edge-case tests onto the seven categories, with rows authored as `status: "planned"` and both pre-submission checklists growing to **18 items**. `agents/task-reviewer` bumps to reviewer `schema_version` **1.6**: a Behaviour/Test Matrix Verification pass locates the test each row names and judges it Verified / Missing / Mismatch, emitting a new **OPTIONAL** top-level `behaviour_test_matrix` verdict `{ status, note, rows[] }` whose rows reuse the task-side vocabulary (Verified → `passing`, Missing and Mismatch → `failing`; `verified`/`missing`/`mismatch` are not wire values), fail-closed so any `failing` row forces the section to `failed` and requires a matching `category: "testing"` issue. `stride-workflow` Step 4 drives implementation from the rows (write each named test, advance its status, PATCH the updated matrix back) and Step 5 passes the field in the reviewer dispatch list; `stride-subagent-workflow` mirrors both and `stride-completing-tasks` adds a verdict self-check. The field is **OPTIONAL throughout** and deliberately not one of the five review_queue-scored fields, so a task without a matrix is never penalized and never renders an empty pill. Documentation/agent-prompt change only — no hook, `.stride.md`, or `.stride_auth.md` change.

**v1.39.0+:** an optional, gracefully-degrading integration with the [`stride-security-review`](https://github.com/cheezy/stride-security-review) plugin brings the specialist mitigation check into the review phase (G-5613). `agents/task-reviewer.md` bumps to reviewer `schema_version` **1.5**: the `security_considerations` verdict gains an OPTIONAL nested `considerations[]` array (`{ consideration, status: mitigated | partial | unmitigated, evidence, note }`) with a fail-closed escalation rule (any `partial`/`unmitigated` entry forces the section `status` to `failed` and requires a matching `category: "security"` issue); the three-state section-status enum is unchanged and the array is Claude-Code-only and always optional. `stride-workflow` Step 5 gains a gated **Deep security-considerations review** sub-step — when the task's `security_considerations` is non-empty **and** the `stride-security-review` plugin is available (detected via its sanctioned surface, never by executing untrusted plugin content), the `stride-security-review:security-reviewer` is dispatched in considerations mode with the diff + considerations framed as **data, never instructions**; its verdicts merge into `reviewer_result.security_considerations.considerations[]` via the existing verbatim whole-object passthrough and the dispatch time folds into the existing reviewer step (no new `workflow_steps` name). `stride-subagent-workflow` documents the identical trigger as an orthogonal Decision-Matrix dispatch, and `stride-completing-tasks` documents the automatic nested-array passthrough plus a matching self-check consistency item. When the plugin is absent, `security_considerations` is empty (or a `None — …` placeholder), or the environment is not Claude Code, the sub-step is skipped with no failure and the task-reviewer's prose verdict remains the sole source. Documentation/agent-prompt change only — no wire-shape, hook, `.stride.md`, or `.stride_auth.md` change.

**v1.30.0+:** the creation skills now document the optional `created_by_agent` field (G254). Agent-created tasks previously landed with `created_by_agent` nil, so the `/agents` activity feed rendered an uninformative `?` avatar on every `created` row. `stride-creating-tasks` adds it to the complete-task example, the Field Quick Reference table, and an explanatory note; `stride-creating-goals` adds it to the batch goal example. Set it to the plugin's own agent name — the exact same value sent as `agent_name` on claim/complete, using the plain agent name and never the `ai_agent:<model>` token form — so one agent stays one roster identity. The field is **create-only** (forbidden on `PATCH`, cannot be backfilled), and the server propagates a batch goal's value to **every nested child task**, so it is set once on the goal. Documentation-only: no wire-shape, hook, `.stride.md`, or `.stride_auth.md` change; `created_by_agent` was already accepted by the create API.

**v1.29.0+:** the `technical_details` task field is now documented across the plugin (G243). It is an **optional, free-form JSON object** a task may carry for any additional technical context that does not fit the structured fields — data shapes, gotchas, key decisions, reference links. Unlike `testing_strategy`, it has **no fixed keys**, and it is **not** one of the five review_queue-scored fields (`acceptance_criteria`, `testing_strategy`, `security_considerations`, `pitfalls`, `patterns_to_follow`), so a blank `{}` is never a scoring gap. The creation contracts (`stride-creating-tasks`, `stride-creating-goals`), the enrichment and decomposition guidance (`task-enricher`, `stride-enriching-tasks`, `task-decomposer`), and the workflow/exploration references agents read while working a task (`stride-workflow` Step 1, `task-explorer`) all describe the field consistently — optional, free-form, never fabricated, with a no-secrets reminder. Documentation-only: no wire-shape, hook, `.stride.md`, or `.stride_auth.md` change.

**v1.28.0+:** the claim-time `TASK_BASE_REF` is always refreshed (G224). The PostToolUse hook records the HEAD commit at claim time in `.stride-env-cache`, and the `after_doing` flow diffs the working tree against it to build `changed_files`. The cache was only written when the claim response JSON parsed out of `tool_response.stdout`; an oversized claim response (~200KB+) is persisted to a file by Claude Code, leaving only a `Full output saved to: …` notice in stdout — so the parse failed, the write was skipped, and a **stale** `TASK_BASE_REF` from a previous claim survived, making `changed_files` span unrelated commits (D60 showed 27 files for a 3-file task). **`hooks/stride-hook.sh`** (W1086) adds a persisted-output file fallback (recover the API JSON from the saved file — validated as a regular file and parsed with `jq` only, never sourced/eval'd; space- and quote-tolerant path) and makes the base-ref refresh **unconditional** on every claim: when no task JSON is obtainable it still rewrites `TASK_BASE_REF` to the current HEAD and clears `.stride-changed-files.json` / `.stride-diff-upload-state`, preserving `TASK_` identity lines (silent no-op in a non-git dir). **`hooks/stride-hook.ps1`** (W1087) ports the same behavior for Windows parity (the ps1 hook previously never wrote `TASK_BASE_REF`) and fixes a pre-existing `Set-StrictMode` hazard where reading `.data` on the stdout-wrapper object threw and aborted caching. Bash Test Group 14 and PowerShell Test Group 10 mirror test-for-test (bash 237, PowerShell 172 green). No wire-shape, hook-timeout, `.stride.md`, or `.stride_auth.md` change. This pin also carries the previously-unreleased **v1.26.0** (D65/D66) and **v1.27.0** (D67) plugin changes.

**v1.25.0+:** the changed-files diff upload survives the `after_doing` timeout (W1093-W1096). The after_doing quality gate (tests with coverage, credo, sobelow, auto-commit) shares one hook timeout with the per-file diff upload, which used to run only **after** every gate command succeeded — a slow gate let the timeout kill the process before the upload, silently losing the task's diffs. Four defense layers: **(1)** `hooks/stride-hook.sh` now captures and PUTs the snapshot **before** the first after_doing section command executes (gated internally on the GLOBAL `$HOOK_NAME` so the after_goal reuse stays inert; degraded captures still write a best-effort `[]` and never block the gate), keeping the post-commands call as a refresh so tree-mutating gate commands still surface in the final snapshot. **(2)** A self-heal layer: `finalize_after_doing` records each PUT outcome in `.stride-diff-upload-state` (task id + HTTP code only, never credentials), and the before_review hook — a fresh PostToolUse timeout budget — verifies that state, re-capturing against `TASK_BASE_REF` and re-PUTting when it is missing, names a different task, or recorded a non-2xx; a healthy 2xx short-circuits before credential resolution. The upload moved into a shared `upload_changed_files_snapshot` helper preserving the D61 base64 envelope; the state file is cleaned at the claim refresh and the after_review cleanup. **(3)** `hooks/stride-hook.ps1` Windows parity — the same early upload, state file, and before_review retry, with the claim refresh and after_review cleanup also clearing the snapshot (the ps1 script has no capture step, so a stale snapshot must never re-upload under a new task id) — plus a repair of the ps1 test suite, which failed at baseline on pwsh 7.6 (reserved `$Input` param name, StrictMode `.Count`-on-null crash, `Start-Process -ArgumentList` argv mangling on Unix .NET that silently lost all section-command output — replaced with `ProcessStartInfo.ArgumentList` and concurrent `ReadToEndAsync` drains — invalid hand-rolled JSON dropping the Bearer token, a listener-startup race, and stale pre-D61 assertions). **(4)** Both Bash hook timeouts in `hooks/hooks.json` raised 120 → 300 seconds (the Skill-matcher gate stays at 10s), with a README subsection documenting the after_doing time budget as a shared ceiling, the kill behavior, and the trim-or-fork recommendation. Test suites: bash 198 assertions green (new Groups 12+13), PowerShell 131 green (new Group 9). Projects should gitignore `.stride-diff-upload-state` alongside `.stride-env-cache` and `.stride-changed-files.json`.

**v1.24.0+:** review reports must be delivered complete — **NO EXCEPTIONS** (goal G222). Every part of a dispatched review report is now mandatory on every task completion: all review information the task supplies (`security_considerations`, `testing_strategy`, `patterns_to_follow`, `pitfalls`, `acceptance_criteria`) must be passed to the reviewer, and the reviewer's entire structured output (every project check and every section verdict) must reach the server intact — not for small or trivial tasks, never with a promise to fix it later. This is the plugin-side source fix for the recurring defect where a task's security considerations came back **`not_assessed`** and the project-checks list was silently **truncated** (3 of 26 reached the server). `skills/stride-workflow/SKILL.md` (W1072) now passes **every** review field the task supplies to the reviewer (it previously listed only four, omitting `security_considerations`, so it came back `not_assessed`); `agents/task-reviewer.md` (W1073 / W1076) reserves `not_assessed` **strictly** for a section the task itself left empty and single-sources its input contract; the `reviewer_result` passthrough (W1074, in `skills/stride-workflow/SKILL.md` + `skills/stride-completing-tasks/SKILL.md`) is now a mechanical **whole-object copy** with a count-checked self-check (submitted `project_checks` count must equal the reviewer's); and `skills/stride-completing-tasks/SKILL.md` (W1075) adds a mandatory **pre-submission self-check hard gate** that refuses to submit a thin or task-inconsistent report. Documentation/agent-prompt change only — no wire-shape, hook, `.stride.md`, or `.stride_auth.md` change; the Kanban server independently hard-rejects an incomplete or task-inconsistent report (goal G221), so the two halves enforce completeness from both ends.

**Install:**

```bash
/plugin install stride@stride-marketplace
```

**Skills (all MANDATORY — each contains required API fields only documented in that skill):**

- `stride-workflow` — RECOMMENDED entry point — single orchestrator for the complete task lifecycle (claim → explore → implement → review → complete)
- `stride-claiming-tasks` — MANDATORY before `GET /api/tasks/next` or `POST /api/tasks/claim`
- `stride-completing-tasks` — MANDATORY before `PATCH /api/tasks/:id/complete`
- `stride-creating-tasks` — MANDATORY before `POST /api/tasks` (work tasks or defects)
- `stride-creating-goals` — MANDATORY before `POST /api/tasks/batch` (goals with nested tasks)
- `stride-enriching-tasks` — MANDATORY when task has empty `key_files` / `testing_strategy` / `verification_steps`
- `stride-subagent-workflow` — MANDATORY after claiming any task, before implementation (Claude Code only)

**Agents:**

- `stride:task-enricher` — Explores the codebase to fill in a sparse task before it is claimed, discovering `key_files`, `patterns_to_follow`, `testing_strategy`, and `verification_steps` for the orchestrator to PATCH onto the task
- `stride:task-decomposer` — Breaks goals and large tasks into dependency-ordered child tasks
- `stride:task-explorer` — Targeted codebase exploration after claiming a task, guided by `key_files` and `patterns_to_follow`
- `stride:task-reviewer` — Pre-completion code review validating changes against `acceptance_criteria` and `pitfalls`
- `stride:hook-diagnostician` — Analyzes hook failure output (structured JSON or raw text) and returns a prioritized fix plan

**Per-File Diff Capture (v1.14.0+):**

The plugin implements the G148/W719 per-file diff JSON contract. The `after_doing` hook captures `git diff $TASK_BASE_REF..HEAD` per changed file, truncates each diff at exactly 500 lines with the contract marker, emits the binary placeholder for files git marks binary in `--numstat`, and writes the resulting JSON array to `.stride-changed-files.json` for the completion payload to carry as the optional `changed_files` field. Reviewers see per-file unified-patch text inline in the Stride review queue. The field is optional — legacy completions without `changed_files` continue to validate.

**v1.14.1+:** the canonical API Request Format example body and the pre-completion verification checklist in `stride-completing-tasks/SKILL.md` both surface `changed_files` directly, so agents reading the standard example are prompted to embed the snapshot during normal payload assembly (no need to find the dedicated optional section first).

**v1.16.0+:** the `after_doing` hook PUTs `.stride-changed-files.json` to `/api/tasks/:id/changed_files` immediately after writing it to disk — URL and Bearer token are extracted from the intercepted agent completion command, fire-and-forget, no new env vars or `.stride_auth.md` read. PowerShell mirror in `hooks/stride-hook.ps1`. The agent's completion body no longer needs `changed_files` on a Stride server v1.16.0+ deployment (kanban W777 endpoint). The legacy inline-cat pattern is preserved in SKILL.md under a "Legacy inline pattern" subsection for back-compat against ≤ v1.15.x server deployments — both modes coexist.

**v1.22.0+:** the `after_doing` hook PUTs the snapshot to `/api/tasks/:id/changed_files` as a transport-encoded envelope — `{"changed_files":{"encoding":"base64","data":"<single-line-base64>"}}` — instead of the raw `{"changed_files":[...]}` array, so an edge request filter (WAF) in front of the Stride server cannot misread a dense code diff as an attack payload and silently drop the upload (which left `changed_files` empty in the review queue). The server decodes the base64 back to the identical list. When `base64` is unavailable the hook falls back to the raw array shape (the object wrapper is preserved on both paths so the value never lands at `params['_json']` as NULL), and a non-2xx upload response is now surfaced as a stderr warning instead of being discarded (non-fatal to completion; the bearer token is never logged). PowerShell mirror updated to match. Requires the Kanban server to accept the `base64` / `gzip+base64` envelope on `/changed_files` (decoded with bounded streaming inflate + size guards), which ships independently in the kanban repo. Source: D61.

**v1.22.1+:** fixes a data-loss defect where the completion flow dropped `reviewer_result.project_checks`. `skills/stride-workflow/SKILL.md` Step 6 built `reviewer_result` from a hand-maintained enumerated copy-list that omitted `project_checks`, so the reviewer's CODE-REVIEW.md per-bullet audit was silently dropped on completion and the Kanban review queue's **Code review** panel (gated on `reviewer_result["project_checks"]`) rendered nothing. The guidance is now a **verbatim passthrough** — copy the reviewer's entire parsed JSON object into `reviewer_result` and overlay only the legacy summary fields — so any key the schema gains later flows through automatically with no consumer edit. `agents/task-reviewer.md` gains a **consumption invariant** making it the single place the structured key-set is enumerated (consumers passthrough verbatim, never re-enumerate), reinforced by a `stride-completing-tasks` completion-checklist guard. Documentation/skill-instruction change only — no wire-shape change; `project_checks[]` already existed (v1.18.0+) and was already rendered. The kanban-repo regression test ships independently. Source: D63 / W1049 / W1051 (goal G218).

**v1.17.0+:** adds the `## after_goal` hook section — fires after the parent goal's final child task completes, blocking, 60s timeout, same single-bash-fence parsing rule as the four existing hooks. The executor forwards `GOAL_ID` / `GOAL_IDENTIFIER` / `GOAL_TITLE` / `GOAL_DESCRIPTION` env vars from the server-supplied `hook.env` (server is the single source of truth — the executor never invents or looks up these values client-side). A missing `## after_goal` section parses as a clean no-op so older `.stride.md` files keep working; the server-side grace-window path bridges agent runtimes that don't speak the after_goal protocol. SKILL.md gains a five-row hooks reference table (timing/blocking/timeout columns), a Hook Environment Variables matrix, and a Canonical Hook Examples block — the hook is general-purpose (Slack notifications, artifact archival, release pipelines, project-level smoke tests are equally valid uses). Server-side companion (kanban-side): W498 emits `[:kanban, :api, :after_goal_delivered]` telemetry on each real delivery, W499 surfaces an after_goal adoption tile (distinct boards reporting in trailing 7d, grace-worker excluded), W500 surfaces p50/p95 goal-to-Done latency (14d window, server-side timestamps, grace-worker excluded).

**v1.17.3+:** strengthens the three task-authoring SKILL.md files (`stride-creating-tasks`, `stride-enriching-tasks`, `stride-creating-goals`) to name the four review_queue-scored fields explicitly — `acceptance_criteria`, `testing_strategy`, `pitfalls`, `patterns_to_follow` — and the empty-pill consequence of omitting any of them at completion. Each skill gains a top-of-file ⚠️ REVIEW QUEUE SCORING callout (co-located with the existing MANDATORY / Iron Law callouts), plus reinforcement inside the skill's existing Red Flags - STOP, Rationalization Table, Phase 4 checklist, or Task Nesting Rules structures (no new top-level sections introduced). `stride-enriching-tasks` promotes the four fields to individual mandatory-for-review items in the Phase 4 16-item pre-submission checklist (replacing the prior single-line bundling); `stride-creating-goals` stresses that nested tasks are graded individually — no "it's just a subtask" discount, and the goal-level `description` does not satisfy nested-task fields. Content-only release: no hook script, parser, env-var matrix, API field shape, or workflow step changed.

**Commands (v1.20.0+):**

Two slash commands create Stride work from existing project markdown. `/stride:create-tasks [--dir <path>] [task description]` and `/stride:create-goals [--dir <path>] [goal description]` (alias `--context`; also accepts `--dir=<path>`) load the `.md` files in the given directory as a read-only context bundle and route through the `stride-workflow` orchestrator — which dispatches `stride-creating-tasks` / `stride-creating-goals` with the bundle forwarded verbatim. They never call the creation sub-skills directly; the activation marker and sub-skill gate still apply. A `--dir` set but missing errors (non-zero); an empty directory warns and continues. Context augments interactive intent and never excuses a blank required field.

**Explorer/Reviewer Result Enforcement (v1.9.0+):**

Every `/complete` call must include `explorer_result` and `reviewer_result` objects — either a dispatched subagent result or a self-reported skip with a reason from the fixed enum. Summary must be at least 40 non-whitespace characters. Server validates in grace mode (log + succeed) until the strict flag is flipped after all plugins release, then rejects with 422.

**Automatic Hook Execution (v1.5.0+):**

The plugin ships Claude Code hooks that automatically execute `.stride.md` commands without permission prompts. When the plugin is enabled, Stride API calls are detected and the corresponding hook section runs as a shell process on the harness — bypassing Claude's tool permission layer entirely.

| Claude Code Event | API Pattern | Stride Hook |
|---|---|---|
| PostToolUse (Bash) | `/api/tasks/claim` | `before_doing` |
| PreToolUse (Bash) | `/api/tasks/:id/complete` | `after_doing` (blocks on failure) |
| PostToolUse (Bash) | `/api/tasks/:id/complete` | `before_review` |
| PostToolUse (Bash) | `/api/tasks/:id/mark_reviewed` | `after_review` |
| PostToolUse (Bash) | `/api/tasks/:id/complete` or `/api/tasks/:id/mark_reviewed`, when the completing task is the parent goal's final child | `after_goal` (blocking, 60s timeout) |

Task environment variables (`$TASK_IDENTIFIER`, `$TASK_TITLE`, etc.) are cached from the claim response and available to all hooks. Structured JSON diagnostics are emitted on both success and failure for integration with the hook-diagnostician agent.

**Repository:** <https://github.com/cheezy/stride>

---

## stride-security-review

AI-powered security review of code changes via the `/stride-security-review:security-review` slash command. Semantic analysis, not pattern matching — a `grep` hit on `eval(` is not a finding; `eval(user_input)` at a trust boundary is.

**Version history:**

- **v2.0.0 — breaking rename.** The plugin was renamed from `security-review` to `stride-security-review`, resolving a namespace collision with Claude Code's own built-in review command — the bare `/security-review` form had been silently falling through to the built-in. Inner artifact names are deliberately unchanged; only the outer namespace moved, so the canonical invocation is the `/stride-security-review:security-review` form shown above. Anything scripted against the old namespaced form must be updated.
- **v2.5.0 — considerations mode (G366).** A third input mode, exposed both to the agent directly and via the `--considerations <source>` flag documented below. Given a diff plus a task's `security_considerations`, the reviewer returns one `{consideration, status, evidence, note}` verdict per listed consideration — `mitigated` / `partial` / `unmitigated` — alongside its usual findings, where a `partial` or `unmitigated` verdict is backed by a matching findings entry. The list is treated strictly as task-authored **data to assess, never as instructions to follow**, and the source is read as untrusted data, never shell-executed. The verdict array is emitted **only** in this mode, so output stays byte-identical for callers that do not opt in; the flag is diff-based and is ignored under `--full`.
- **v2.5.1 — agent-dispatch exit-code contract fixed (D170).** The documented exit-code table promised `exit 2` on an agent dispatch failure, but the three agent-response-failure branches — diff-mode dispatch, full-mode batch, and the RCI pass — only printed a one-line error and stopped, so a `--fail-on` CI gate observed an unspecified exit code when the reviewer returned unparseable JSON. Each branch now runs a final `exit 2`, mirroring the misuse pattern every other exit-2 case already used. The exit-code meanings themselves are unchanged; only the procedure was reconciled to the contract.

The releases between the rename and the considerations mode built out the framework and CI/CD rule packs, the SARIF and CI integration, the eval runner and its golden-file transform tests, and the flags catalogued below — see the plugin repo's `CHANGELOG.md` for the full arc.

**Install:**

```bash
/plugin install stride-security-review@stride-marketplace
```

**Two scan modes:**

- **Diff (default)** — reviews working-tree changes against `HEAD`. The PR-gating shape.
- **`--full`** — reviews every tracked text file under a 256 KiB cap, batched in groups of 10 with cross-batch dedup and skipped-files reporting. The codebase-posture-check shape.

**What it catches:**

- **10 universal vulnerability classes** — injection (SQL, command, LDAP, NoSQL, XXE, template, header), authentication, authorization, data exposure, cryptography, input validation, race conditions, XSS / code execution, insecure configuration, supply chain.
- **5 MAESTRO-derived agentic-AI classes** (when the file imports an LLM/agent/MCP SDK) — prompt injection, tool abuse, agent trust boundary, model output execution, vector-store poisoning.
- **Seven framework rule packs** (Android/Kotlin, Django/Python, Express/Node.js, iOS/Swift, Phoenix/Elixir, Rails/Ruby, React/Next.js — covering the full mobile + web + Node ecosystem). Highlights:
  - **Android/Kotlin** — exported components without signature permission, WebView `setJavaScriptEnabled` + `addJavascriptInterface` RCE, cleartext traffic, SharedPreferences storing credentials, SQLite injection, trust-all `HostnameVerifier` / `X509TrustManager`. Activates on `.kt` / `.java` / `.gradle` imports OR `AndroidManifest.xml` content.
  - **Django/Python** — `mark_safe`, `extra` / `raw()` SQL interpolation, CSRF disabled, production settings hardening (DEBUG / ALLOWED_HOSTS / SECURE_*), mass-assignment, open-redirect, deserialization, SSRF with cloud-metadata focus, DRF `ModelSerializer fields='__all__'`.
  - **Express/Node.js** — reflected XSS via `res.send(req.query.x)`, `eval` / `Function()` / `vm.runInNewContext` RCE, `child_process.exec` shell injection, Mongoose `Model.find(req.body)` NoSQL operator injection, prototype pollution via `lodash.merge` / `_.set`.
  - **iOS/Swift** — sensitive data in `UserDefaults` instead of Keychain, WKWebView file access + JS bridge, ATS `NSAllowsArbitraryLoads`, URLSession trust-all delegate, deep-link missing re-auth. Activates on Swift / Obj-C / Obj-C++ imports OR `Info.plist` content.
  - **Phoenix/Elixir** — `Phoenix.HTML.raw/1`, missing `force_ssl`, CSRF, `Ecto.Query.fragment` interpolation, LiveView scope, changeset mass-assignment, `Phoenix.Token` replay, `System.cmd` shell, LiveView `allow_upload` guards, open-redirect.
  - **Rails/Ruby** — `html_safe` / `raw`, `find_by_sql` / `connection.execute` interpolation, `protect_from_forgery` disabled, `params.permit!`, `eval` / `send`, open-redirect, `Marshal.load` / `YAML.load` deserialization, missing `authenticate_user!`, unfiltered `render json: @user` leaking `password_digest` / `*_token` / `*_secret`.
  - **React/Next.js** — `dangerouslySetInnerHTML` XSS, Next.js API route missing auth, redirect open-redirect, `<a href={user_url}>` with `javascript:` scheme bypass, `getServerSideProps` / Route Handler secret-leak via props.
- **CI/CD pipeline pack** — five rules across eight platforms (GitHub Actions, GitLab CI, CircleCI, Bitbucket Pipelines, Jenkins, Azure Pipelines, Drone, Tekton).
- **Web defense-in-depth pack** — framework-agnostic checks for missing CSP, HSTS, X-Frame-Options, and `Set-Cookie` without `Secure` / `HttpOnly` / `SameSite`.

Every finding carries CWE-ID and OWASP Top 10 references.

**Opt-in flags (composable):**

| Flag | Effect |
|---|---|
| `--full` | Full-codebase scan instead of diff. |
| `--json` | Raw JSON output (mutually exclusive with `--sarif`). |
| `--sarif` | SARIF 2.1.0 (the OASIS spec version, not a plugin version) output for GitHub Code Scanning. |
| `--fail-on <severity>` | Exit non-zero on findings at/above `critical \| high \| medium \| low`. CI gate. |
| `--base <ref>` | Diff against `<ref>...HEAD` (three-dot range) instead of working tree against HEAD. The PR-against-base shape. |
| `--maestro` | Adds MAESTRO 7-layer classification to each finding plus a `By MAESTRO layer` summary section. |
| `--rci [N]` | Run N (1-3) recursive critique-and-refine passes after the initial review. |
| `--baseline [PATH]` | Suppress acknowledged findings from a baseline file. |
| `--update-baseline` | Write a new baseline from the current run. |
| `--patches` | Emit surgical-fix unified-diff `patch` field per finding when one exists. |
| `--considerations <source>` | Considerations mode: verify a task's `security_considerations` (a file, one per line, or an inline string) were mitigated by the diff — each comes back `mitigated` / `partial` / `unmitigated` with evidence. Diff-based; omitted when not set. |

**Ships:**

- A reference GitHub Actions workflow (`.github/workflows/security-review.yml`) that gates PRs via a belt-and-suspenders `jq + claude_exit` check.
- A TAP 13 eval runner (`scripts/run_eval.sh`) over the fixture corpus with both positive-control (assert finding fires) and negative-control (assert finding does NOT fire) coverage. Includes its own CI workflow that re-runs the eval on every change to the agent prompt.

**Loosely based on** [`anthropics/claude-code-security-review`](https://github.com/anthropics/claude-code-security-review).

**Repository:** <https://github.com/cheezy/stride-security-review>

---

## stride-ideation

Turn a fuzzy idea into shipped Stride tasks via two slash commands. Replaces `superpowers:brainstorming` for Stride projects; the two coexist for non-Stride projects where brainstorming remains the right fit.

**Install:**

```bash
/plugin install stride-ideation@stride-marketplace
```

**Two slash commands:**

### `/stride-ideation:ideate`

Drives an interactive round-based question loop and writes a committed requirements markdown doc — the terminal state, never auto-funnels into the next step. Up to 4 batched `AskUserQuestion` questions per round, mandatory round-3 framing checkpoint, mandatory round-4 premortem, and a mandatory round-4-to-reviewer **challenge gate** (v0.10.0). Hard-gated on seven required sections (Goal, Problem, Outcome, Assumptions, Constraints, Non-goals, Success Metrics) with shape requirements:

- **Assumptions** must be ranked highest-to-lowest risk, riskiest marked `(R)`.
- **Success Metrics** must include both leading and lagging indicators.

An advisory `requirements-reviewer` subagent runs after the doc is assembled and surfaces gaps before commit.

**Profiles (v0.4.0+):** select the round structure and reviewer rubric with `--profile`.

| Profile | Round structure | Adds |
|---|---|---|
| `lean` (default, byte-for-byte v0.3.0) | 4 rounds | Goal / Problem / Outcome / Premortem |
| `product` | Same + JTBD four-forces in Round 1 | Optional "Concrete Example" section |
| `discovery` | Same + Why-now and Alternative-options in Round 2 | — |
| `lean-startup` (v0.5.0) | Same + mandatory Round-5 MVP-design batch | Optional "MVP / Validation experiment" section + two reviewer checks (MVP section presence, falsifiable success/failure criteria) |

The `lean-startup` profile is anchored on the `(R)`-marked Assumption — source techniques: Eric Ries (Lean Startup), Steve Blank (Customer Development), Giff Constable (Riskiest Assumption Test).

**Session experience (v0.8.0+):** the round loop is now guided, recoverable, and human-in-control. Additive — a flag-free or `--profile=lean` run produces the same committed doc.

- **Per-round recap** — before every round, a display-only status (`solid` / `thin` / `empty`) for the seven gated sections; never changes the gate, round order, or `<=4`-question budget.
- **"I'm not sure — propose candidates"** — every gated-section and forcing question carries this option; picking it makes the skill propose 2–4 topic-tailored candidates with rationales. A candidate never satisfies the gate until you confirm it.
- **Profile recommendation** — when `--profile` is omitted, a recommended-first question (lean default) runs before the rounds; explicit `--profile` skips it.
- **`--input <path>` brain-dump seed** — reads a freeform notes file read-only and pre-fills draft sections, then focuses the rounds on gaps. Distinct from and composable with `--continue`; the file is never modified, moved, or committed.
- **Draft autosave & resume** — the in-progress draft is autosaved after every round to a gitignored `.stride/` scratch file; resume-or-fresh is offered for the same slug on start; the scratch is deleted after a successful commit. Never holds the Stride API token.
- **Challenge gate (v0.10.0)** — after the round-4 premortem (and the Round-5 MVP batch under `lean-startup`) and before the reviewer pass, a mandatory, profile-independent gate stress-tests the design via four components: an assumption-confidence audit (rate every assumption `high`/`medium`/`low`), a blind-spot scan, two distinct alternatives, and a cost/risk/complexity/timeline trade-off. Advisory with an explicit "Challenge nothing — write as-is" option; it never blocks the write. Confidence ratings fold into Assumptions in place; blind spots, the two alternatives, and the trade-off fold into a new optional "Design challenge" section (not one of the seven gated sections).
- **Reviewer decision** — advisory `requirements-reviewer` findings are surfaced as a multi-select (severity-tagged) with an explicit "Address none — write as-is" choice feeding the single refinement round. At most one refinement round; the reviewer never blocks the write.

### `/stride-ideation:stridify`

End-to-end pipeline from requirements doc to created Stride goals. Validates the seven required sections, preflights auth from `.stride_auth.md`, dispatches the `requirements-decomposer` subagent (canonical batch shape embedded; two-reason multi-goal split rule of code-coupling + ~10-task soft cap), stamps `source_spec` and `source_spec_sha256`, writes and commits a sibling timestamped batch JSON for audit, then strips local-audit fields and POSTs to `/api/tasks/batch` with token hygiene (no `curl -v`, no token in logs). Renders the created G/W identifiers.

**Resilience model (v0.7.0+):** four layers that protect against transient Anthropic API capacity spikes and oversized many-surface decomposition runs. Activates only on the relevant trigger — v0.6.0 behavior is preserved byte-for-byte on the happy path.

| Layer | Trigger | What happens |
|---|---|---|
| Preflight advisory | Doc enumerates >3 surfaces under `## Decomposition seams` AND `--goal` is unset | One-line stderr suggestion to use `--goal`; never blocks |
| `--goal <name|index>` | User invokes per-surface dispatch | Prompt scoped to one seam (integer-index OR slug-match; both `--goal value` and `--goal=value` forms); per-goal batch JSON sits side-by-side via `sti_unique_path` |
| Subagent dispatch retry | HTTP 529 / transient network / `overloaded` classification | 3 attempts, ~30s / ~90s backoff; terminal classifications (bad subagent, contract violation, hard 4xx) fail fast on attempt 1 |
| Retry-exhaustion fallback | 3 consecutive transient failures | Writes `<source-stem>-decomposer-prompt.md` sibling with the assembled prompt, verbatim last error, and recovery README; **Stride API POST is NOT attempted** |

The Stride API POST itself is still not retried — partial-batch idempotency is not guaranteed; the user remains the retry mechanism on a 4xx/5xx.

**Preview-and-approval gate (v0.8.0+):** before the POST, `/stridify` renders the decomposed goal/task tree (each goal title, its task count and titles, and the cross-goal claim order from `decomposition_notes`) and requires explicit human approval. The batch JSON is written and committed *before* the gate, so a decline stops cleanly (exit 0) with the audited artifact intact and no POST. Pass `--yes` / `--auto-approve` (explicit only, never inferred) to bypass the gate and preserve the historical fire-and-forget behavior byte-for-byte for scripted callers. The preview reads only the on-disk JSON and never prints the API token.

**Scored-field alignment and agent attribution (v0.11.0+):** the decomposer contract, the calibration fixtures, and the batch validator now all model and enforce the five review-queue scored fields, and shipped goals are attributed. `agents/requirements-decomposer.md`'s canonical task skeleton carries all five (`acceptance_criteria`, `testing_strategy`, `security_considerations`, `pitfalls`, `patterns_to_follow`), with the none-with-reason convention annotated on `security_considerations`, realistic values in every worked example task, and a "never omit these" callout naming the empty-pill consequence — previously `testing_strategy` appeared in the skeleton but in no example and `security_considerations` appeared once as a type-shape aside, so an agent following the definition would never emit it (W1460). The calibration fixture batches, which are the reference for good decomposer output, now carry all five on every task with scenario-realistic values; the change is strictly additive — task counts, titles, dependencies, `source_spec` and `source_spec_sha256` are byte-identical (W1462). `lib/validate_batch.py` gains a **fatal `length_limit` check** that fails with the offending JSON path and length when a goal or task `title`, or any `security_considerations` element, exceeds 255 Unicode code points — exactly the fields the server binds to `varchar(255)`, with `pitfalls` elements and `key_files` notes deliberately excluded because they are unbounded JSONB server-side and checking them would reject batches the API accepts — plus an **advisory** scored-field completeness pass that warns on stdout, never changing the exit code, for any task whose scored field is missing or empty (warnings go to stdout because `/stridify` Step 8a captures only stderr) (W1467). And `/stridify` Step 8b now stamps `created_by_agent` on **every goal object** with the running agent's plain name — the same value sent as `agent_name` on claim and complete, with the server propagating the goal's value to nested child tasks — before the write and commit, so the audited batch artifact and the POST body carry it identically; the field is create-only and forbidden on `PATCH`, so every batch shipped without it was permanently unattributed in the `/agents` feed, and `lib/strip_audit_fields.py` is guarded against ever adding it to the strip set (W1461). The release also corrects the stale doc claims its accompanying audit surfaced — `drift_check.py` is documented as a standalone consistency tool `/stridify` deliberately does not invoke (W1466), the lib scripts stop citing the pre-merge `/stride-ideation:decompose` and `:ship` commands (W1465), the README defers to the CHANGELOG's topmost entry instead of naming a current release and describes the smoke-test output shape with `0 failed` as the invariant (W1464), and `requirements-reviewer.md`'s check count is made self-consistent while `commands/ideate.md` promotes the round-4 premortem and the `lean-startup` Round-5 MVP batch to their own enforcement bullets (W1463).

**Repository:** <https://github.com/cheezy/stride-ideation>

---

## stride-lite

A lightweight companion plugin to Stride — produces Stride-shaped **goal and task markdown documents on disk** from a free-text prompt plus an optional requirements directory. No API calls, no kanban setup, no auth files. Just markdown.

Useful when you want the Stride field discipline (acceptance criteria, key files, pitfalls, testing strategy, dependencies) without committing to the full Stride server workflow. The documents it produces can be reviewed, edited, or later submitted to a real Stride deployment by hand or by tooling — this plugin never does so itself.

**Install:**

```bash
/plugin install stride-lite@stride-marketplace
```

**Three slash commands:**

- `/stride-lite:create-goal <prompt>` — decomposes a free-text prompt into a goal plus 1–8 child tasks (capped at 8) and writes them to `docs/implementation/PENDING/<slug>/` (`goal.md` plus `taskN.md` per child).
- `/stride-lite:create-task <prompt>` — renders a single task markdown file at `<output-dir>/tasks/<slug>.md`. No goal wrapper, no children.
- `/stride-lite:init` — scaffolds a project-local `.stride_lite.md` with an email field plus three hook sections (`## before_task`, `## after_task`, `## after_goal`). Shape matches the full Stride plugin's `.stride.md` so snippets transfer across plugins.

**Workflow skill:**

- `stride-lite-workflow` — file-based equivalent of the full Stride plugin's `stride-workflow`. Walks a goal directory through an eight-step task lifecycle (select → before_task → explore → implement → after_task → review → review-loop decision → completion + archive), dispatching the two read-only subagents at the right moments. Never POSTs to any API.

**Agents:**

- `stride-lite:create-decomposer` — Decomposes a free-text prompt into a goal + child tasks YAML. No API calls.
- `stride-lite:task-explorer` — Read-only codebase enrichment (Read / Grep / Glob; no Bash, no WebFetch). Appends `## Exploration Report` to the task file.
- `stride-lite:task-reviewer` — Reviews changes against acceptance criteria, pitfalls, patterns, and testing strategy with narrowly-scoped read-only git Bash. Appends `## Review Report` with embedded `reviewer_result` JSON (schema `1.1`).

**Cross-platform hook enforcement (v0.9.0+):**

`hooks/hooks.json` registers Claude Code PreToolUse/PostToolUse handlers that auto-fire the three `.stride_lite.md` sections at the corresponding workflow intercepts:

| Trigger | Phase | Section | Blocking? |
|---|---|---|---|
| `task-explorer` Agent dispatch | PreToolUse | `## before_task` | yes (exit 2 stops the dispatch) |
| `task-reviewer` Agent dispatch | PreToolUse | `## after_task` | yes (exit 2 stops the dispatch) |
| Final-task `goal.md` write with `## Completion Summary` | PostToolUse | `## after_goal` | advisory (PostToolUse cannot roll back the write) |

Cross-platform from day one: `hooks/stride-lite-hook.sh` (POSIX bash, pure-bash JSON parsing, no jq dependency) plus `hooks/stride-lite-hook.ps1` (PowerShell 5.1+, `ConvertFrom-Json` / `ConvertTo-Json`, no module installs). The `.sh` script auto-delegates to `.ps1` on native Windows (OSTYPE unset + COMSPEC set).

**Terminal PENDING → IMPLEMENTED archive move (v0.10.0+):**

After `## after_goal` fires on the final task of a goal, `stride-lite-workflow` Step 8 moves the goal directory from `docs/implementation/PENDING/<slug>/` to `docs/implementation/IMPLEMENTED/<slug>/`. Prefers `git mv` when the directory is git-tracked (preserves rename history); falls back to plain `mv` otherwise. Collision suffixing on the target with `-2` / `-3` / … up to a 1000-iteration safety cap — the archive never overwrites prior entries. After-goal-failure guard (a structured `"status": "failed"` JSON from the harness skips the move so the user can inspect and re-trigger). Non-`/PENDING/` paths (custom `--output-dir`) are left where they are with a stderr warning. Filesystem-mv failures log and exit cleanly — the workflow does not fail because of an archive-move failure. Single-task files under `PENDING/tasks/` are NEVER moved; only goal directories.

**Copy installs ship the hook enforcement layer (v0.11.0+):**

`install.sh` copied every plugin directory *except* `hooks/` — a user who installed by running the script rather than symlinking the checkout got a plugin whose documented PreToolUse/PostToolUse auto-fire simply did not exist: no `hooks.json` for Claude Code to register, no hook scripts to invoke, and a green "installed successfully" message throughout. The copy set now includes `hooks/` via the same `cp -a` idiom, so `stride-lite-hook.sh`'s executable bit survives; the install summary reports the hook scripts copied; and a post-copy sanity check fails loudly — before any success banner — if `hooks/hooks.json` did not land (W1481). **Upgrade note: every copy install made before v0.11.0 is missing the enforcement layer entirely, so anyone who installed via `./install.sh` must re-run the installer** (`./install.sh --force`) after updating; symlinked checkouts were always unaffected. The same release finishes the v0.9.0 harness migration across the surfaces that still described the old semantics: the workflow's Concrete Walkthrough no longer tells the agent to read and execute the `.stride_lite.md` sections by hand — which would have reintroduced double execution — and now ends with the archive move (W1478); the init command and skill stop describing the hook sections as static configuration the plugin never executes, and now state that the Claude Code harness auto-fires them (W1477); phantom `/stride-lite:ideate` and `/stride-lite:decompose` references are purged repo-wide and the decomposer's unjustified `Read`/`Grep` grant is removed, since both dispatch sites pass the requirements text inline and never a file path (W1479); the shipped `/stride-lite:init` goal directory is archived PENDING → IMPLEMENTED via `git mv` per the plugin's own convention (W1483); stale README version labels, the command count, and the create-goal step count are corrected in drift-resistant wording (W1480); and `test/smoke.sh` gains an init-template byte-parity assertion that extracts the canonical block from the init SKILL.md and diffs full content, plus a hook-routing stage that feeds synthetic Claude Code hook payloads through `hooks/stride-lite-hook.sh` as real sandboxed subprocesses to pin the blocking/advisory routing contract in executable fixtures (W1482).

**Gated cross-plugin sub-steps, `SECURITY.md`, and four defect fixes (v0.12.0+):**

`stride-lite-workflow` gains three optional sub-steps, each dispatching an agent from a **different** plugin. All three are skipped cleanly when that plugin is not installed, and a closed gate never fails a task — so an install without `stride-exploratory-testing` or `stride-security-review` behaves exactly as it did before, and the plugin still opens no socket of its own (a dispatch is a local tool call, not a network call). **Step 6a** maps a task's manual-test entries to charters and dispatches `stride-exploratory-testing:explorer` once per charter, with a session budget read from the installed agent's own contract rather than hard-coded here; the authorized-and-non-production affirmative is collected at Step 0 or never, because inferring it from a `localhost` URL *is* supplying it on the user's behalf. **Step 6b** hardens confirmed findings into regression checks that stage outside the test tree and are never reported as passing, since `/harden` holds no test runner. **Step 6c** dispatches `stride-security-review:security-reviewer` in explicitly-declared considerations mode — the declaration is load-bearing, since the agent assumes `diff` mode when the tag is missing and emits verdicts only in considerations mode — and is fail-closed: a consideration is never dispositioned `mitigated` on a verdict set that could not be read. Unlike 6a and 6b it runs on every review iteration, reusing the existing `max_review_iterations` cap rather than adding one; a dispatch that fails outright is the single exception and is a clean skip, because looping on an unavailable third-party agent would let it terminate every task in a goal.

[`SECURITY.md`](https://github.com/cheezy/stride-lite/blob/main/SECURITY.md) states the trust boundary the plugin had never written down: it executes arbitrary user-authored shell commands from `.stride_lite.md`, with the user's privileges, never validated, wrapped, escaped or rewritten. It also records what the activation marker is **not** — a coordination signal forgeable by any local process, never an authorization — so no future change leans on it, and says outright that the PowerShell executor has **not** been verified on a real Windows host, only driven under `pwsh` on macOS.

Four defects are fixed, three of them found by test suites this same release introduced. **D215:** the PowerShell executor read stdin via `@($input)`, which PowerShell populates only for an internal pipeline — for the OS-level pipe the harness actually uses it was empty, so the script hit its own empty-payload guard and exited 0, meaning the documented hook auto-fire had **never worked** on the native-Windows path and, because it exits 0, nothing ever reported it. **D218:** `Start-Process -ArgumentList` re-splits its argument, so `bash` received only the first whitespace-delimited token as its `-c` script; every multi-word hook command silently did nothing while the section reported success. **D216:** the decision matrix counted only markdown table rows under `## Key files`, so a hand-written bullet or numbered list naming three real files matched the `small, 0–1 key files` row and got no explorer, no reviewer and neither hook — while the enrichment gate read the same section as populated, judging one task "has key files" and "zero key files" at once. **D217:** a three-backtick `markdown` fence wrapping a three-backtick `json` block closes early, so the remainder of an agent contract rendered as sample output; the suite now walks fences the way a renderer does, in both backtick and tilde syntax, because neither a fence count nor an unterminated-fence check can see a file that is balanced and still wrongly paired.

**Repository:** <https://github.com/cheezy/stride-lite>

---

## launchdarkly

Generate correct, testable, and removable LaunchDarkly feature-flag code from Claude Code — covers the Java server SDK and the TypeScript SDKs (Node server, client-side JS, React) with skills, scaffolding and flag-removal commands, and an anti-pattern review agent.

**Install:**

```bash
/plugin install launchdarkly@stride-marketplace
```

**Repository:** <https://github.com/cheezy/launchdarkly>

---

## stride-exploratory-testing

Drive structured, charter-based exploratory testing sessions in Claude Code — the discipline that supplies the "explored" half of **Tested = Checked + Explored**. Plan a charter, run a timeboxed session against a running app under an absolute safety boundary, judge results with oracles, and capture findings as an SBTM session sheet and a debrief.

**Surface:**

- **Six skills** — `stride-exploratory-testing` (orchestrator), `chartering`, `heuristics`, `oracles`, `bug-advocacy`, `session`.
- **Seven slash commands** — `/charter`, `/nightmare-headline`, `/explore`, `/pair`, `/recon`, `/debrief`, `/harden`.
- **Two subagents** — `charter-generator` (target + risk → ranked charters) and `explorer` (runs one charter to structured findings under the safety boundary).
- **Fixtures + smoke tests** — worked `example-charters.md`, `example-session-sheet.md`, and `example-debrief.md`, plus a pure-shell `lib/` smoke-test harness.

Encodes established exploratory-testing practice — Cem Kaner, Elisabeth Hendrickson's *Explore It!*, Session-Based Test Management (Bach), James Whittaker's Tours, and the Heuristic Test Strategy Model (Bach).

**(v0.2.0+)** a capability sweep from the plugin's own improvement research: a sixth skill **`bug-advocacy`** (Kaner's RIMGEA — Replicate, Isolate, Maximize, Generalize, Externalize, And say it clearly — plus the **severity** definition the explorer's contract always required and the plugin never had: a four-level `Critical > High > Moderate > Minor` rubric built as an impact ladder of explicit clauses, so two sessions rating the same bug land on the same level); **`/pair`**, the inversion of `/explore`, where *you* drive the app and Claude suggests the next probe, names the `heuristics` lens it came from, and volunteers what the session has neglected (only happy paths, only one tenant, only ASCII) — its division of labour enforced by a tool allowlist holding nothing that can reach your app; **`/harden`**, the first path from *Explored* back to *Checked*, which detects your project's own test framework and drafts a regression check per confirmed bug from its `minimal_repro`, reports what it could not convert rather than guessing, stages drafts under `.exploratory/checks/` instead of writing into your suite, and never claims a draft passes because it holds no test runner; **`.exploratory/` session artifacts** (per-run debriefs and sheets, an append-only charter backlog, and a coverage map that deliberately carries no percentage) that `/charter` reads back so run five proposes different charters than run one; and `SFDPOT` corrected to **`SFDIPOT`** with the missing Interfaces lens. **Breaking for downstream consumers of the `explorer` JSON:** `session_sheet` drops `duration` and `tbs` — wall-clock measures an agent cannot honestly take — for counts plus an agent-native probe budget, and `bugs[]` gains the four RIMGEA products.

**Install:**

```bash
/plugin install stride-exploratory-testing@stride-marketplace
```

**Repository:** <https://github.com/cheezy/stride-exploratory-testing>

---

## Marketplace Structure

```
stride-marketplace/
├── .claude-plugin/
│   └── marketplace.json       # Plugin catalog (the source of truth for plugin versions)
├── CHANGELOG.md               # One entry per catalog release
├── README.md                  # This file
└── LICENSE                    # MIT
```

## Releases and tagging

This catalog carries its own `metadata.version` in `marketplace.json`, independent of the versions it pins. That self-version, this repo's tags, and `CHANGELOG.md` are three records of the same thing, and they only stay honest if every release moves all three together.

**One release is one commit.** Since `v1.64.0` a catalog release is a single commit rather than the two-commit pair described below. That commit should do all four of: bump the plugin's `version` (and `description`, if it changed) in `marketplace.json`, bump `metadata.version`, sync the plugin's row and prose in this README, and add a `## [X.Y.Z]` entry to `CHANGELOG.md`. It is then tagged `vX.Y.Z` and published as a GitHub release. A commit that does only some of those is a half-finished release, not a release.

**The four-step form is newer than the one-commit form.** `v1.64.0` and `v1.65.0` were single commits but did only two of the four steps — they left `metadata.version` behind and wrote no entry, which is the drift `[1.66.0]` had to reconcile. `v1.66.0` is the first commit that did all four. Read "since `v1.64.0`" as when the *shape* changed, and `v1.66.0` as when the *ritual* became complete.

**Earlier releases used two commits, and those are staying as they are.** Through `v1.63.0` the shape was an untagged "Sync … catalog entry" commit that touched only a plugin's own fields, followed by a tagged commit that bumped `metadata.version` (and, for `d69e1ee` alone, also wrote the changelog entry — `4aff71f` and `ea23202` did not). `eeaed1e`, `a44d82b` and `5e33131` are first halves of such pairs — each has its tagged partner (`4aff71f`, `ea23202`, `d69e1ee`). **They are not missed releases and will not be tagged retroactively**; doing so would manufacture a release record for an intermediate state no user ever resolved through.

**Nothing installs through these tags.** `marketplace.json` pins each plugin by a bare repository URL with no ref, so an install resolves to the plugin repo's default branch. The tags and `metadata.version` exist so a human can tell which catalog state they are looking at — which is the whole reason the three records must agree.

**Record divergence; do not silently correct it.** When the records do drift, say so in the changelog entry that fixes it rather than quietly renumbering — the `[1.66.0]` and `[1.67.0]` entries are the worked examples. `v1.64.0` and `v1.65.0` were tagged without a bump or entry; `1.66.0` resumed the sequence and recorded the skip instead of backfilling two releases that already existed on GitHub. When a bump lands without its entry and tag, finish that version rather than bumping past it, or the skipped number is unrecorded forever.

**Check the tag list before choosing a number.** Do not assume the next tag equals the next version — this repo has drifted before. The sibling [`stride-gemini-marketplace`](https://github.com/cheezy/stride-gemini-marketplace) documents its own release conventions, including the rule for what to do when a number is already spent. Note that its catalog tag *mirrors* the pinned extension version, whereas this catalog runs an independent `metadata.version` and mirrors nothing — so borrow the reasoning, not the numbering.

**Known gaps, so the rule above is not read as a description of the present.** The prose-sync gap below is closed as of D189; the rest stand:

- **The per-plugin prose sections track their pins, but only by hand.** They had drifted across several pin bumps: `stride` prose stopped at a `v1.40.0+` clause against a `1.42.0` pin, `stride-ideation` and `stride-lite` at `v0.10.0` against `0.11.0` pins, and `stride-security-review` carried no plugin-version clause at all. Releases had been syncing the table row and skipping the prose. **D189 closed that backlog** — every `##` section *that carries a version clause* now cites its pin as its highest version. Two sections carry no version narrative yet: `launchdarkly` and `stride-exploratory-testing`, the latter still at its initial release. A check written as "highest cited version equals the pin" passes those two vacuously, so it needs a separate "section cites at least one version" assertion to mean anything for them. Nothing enforces any of this automatically, so the third step of the rule above ("sync the plugin's row *and* prose") is the only thing keeping it true; a release that syncs only the row reopens this gap.
- **The table's summary column has become a second cumulative narrative.** The *Available Plugins* summary cell for `stride` is now a single line thousands of characters long carrying every release clause since `v1.18.0` — not a one-line summary, and unreadable as a table cell. Both surfaces are kept deliberately, and neither is canonical: the table is the index a reader scans to choose a plugin, and the `##` section below is the reference documentation they land on afterwards, carrying install, skill, agent, flag and hook detail that exists nowhere else. But the accumulated cells are the wrong home for cumulative detail, and trimming them rewrites rows that are currently accurate — a separate change. Until then, put a release's detail in the prose section and keep its table-cell addition to a short clause.
- **Hardcoded counts in prose go stale silently.** The `stride-security-review` section carried an unscoped, present-tense eval-fixture count that was correct when written and drifted across five pin bumps without anyone noticing; it is now count-free. That plugin's own repo ships [`scripts/check_fixtures.sh`](https://github.com/cheezy/stride-security-review/blob/main/scripts/check_fixtures.sh), which fails its build on exactly this pattern in its own README — nothing equivalent covers this catalog, so the rule here is editorial rather than enforced. **Prefer count-free phrasing in new prose.** If a count is genuinely load-bearing, anchor it to the release it describes: a version-scoped clause like the table's `v2.4.0: 70-fixture eval coverage` stays true forever because it is a historical statement, whereas the same number in present tense is a claim about the current pin and rots on the next one. The per-release bash/PowerShell assertion tallies in the `stride` prose are release-scoped in exactly that way and are left as written. Unscoped present-tense counts do still appear in the per-plugin sections — rule-pack, class, skill and command tallies — and each was verified accurate against the shipped plugin as of the current pins; treat them as live instances to re-verify on a bump, not as cases already handled. The hazard is not specific to fixtures — rule packs, commands, skills, agents and test counts all drift the same way.
- **Tags that predate the four-step ritual often have no changelog entry.** The ritual completed at `v1.66.0`, and roughly half the tags before it were never given one. That set is closed — those releases are recorded rather than backfilled — so it does not grow as new tags are cut; see the `[1.66.0]` and `[1.67.0]` entries in [CHANGELOG.md](CHANGELOG.md) for the audit, its figures as measured then, and the reasoning.

## Support

- **Marketplace issues:** <https://github.com/cheezy/stride-marketplace/issues>
- **Per-plugin repos** are linked in each plugin's section above; report plugin-specific issues there.

## License

MIT License — see [LICENSE](LICENSE).
