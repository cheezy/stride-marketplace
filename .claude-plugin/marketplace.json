{
  "name": "stride-marketplace",
  "owner": {
    "name": "Jeff Morgan",
    "email": "cheezy@letstango.ca"
  },
  "metadata": {
    "description": "Stride task management skills for AI agents",
    "version": "1.44.0"
  },
  "plugins": [
    {
      "name": "stride",
      "source": {
        "source": "url",
        "url": "https://github.com/cheezy/stride.git"
      },
      "description": "Drive the full Stride kanban task lifecycle from Claude Code — claiming, completing, creating and enriching tasks and goals, workflow orchestration, subagent dispatch, and automatic lifecycle hook execution.",
      "version": "1.30.0",
      "strict": true
    },
    {
      "name": "stride-security-review",
      "source": {
        "source": "url",
        "url": "https://github.com/cheezy/stride-security-review.git"
      },
      "description": "AI-powered security review via /stride-security-review:security-review. Two scan modes: diff (default, PR-gating) and --full (codebase posture check, batched with cross-batch dedup and skipped-files reporting). Detects 10 universal vulnerability classes plus 5 MAESTRO-derived agentic-AI classes (prompt injection, tool abuse, agent trust boundary, model output execution, vector-store poisoning). Seven framework rule packs (new in 2.3.0): Android/Kotlin (exported components, WebView JS bridge RCE, cleartext traffic, SharedPreferences credentials, SQLite injection, trust-all SSL), Django/Python (mark_safe, raw/extra SQL, CSRF, production settings, mass-assignment, open-redirect, deserialization, SSRF, DRF mass-assignment), Express/Node.js (reflected XSS, eval RCE, child_process injection, Mongoose NoSQL injection, prototype pollution), iOS/Swift (UserDefaults credentials, WKWebView file access, ATS arbitrary loads, URLSession trust-all, deep-link missing auth), Phoenix/Elixir (raw/1, force_ssl, CSRF, fragment interpolation, LiveView scope, mass-assignment, Token replay, System.cmd shell, LiveView upload guards, open-redirect), Rails/Ruby (html_safe, find_by_sql, CSRF, params.permit!, eval/send, open-redirect, deserialization, connection.execute, missing-auth, JSON-leak), React/Next.js (dangerouslySetInnerHTML, API route missing auth, redirect open-redirect, javascript: href scheme, getServerSideProps secret leak). Plus a multi-platform CI/CD pipeline pack (8 platforms) and a framework-agnostic Web defense-in-depth pack covering security headers (CSP/HSTS/X-Frame-Options/cookie flags). Every finding carries CWE-ID and OWASP Top 10 references. Opt-in flags: --maestro (7-layer threat classification with explicit non-AI mapping), --rci [N] (recursive criticism passes), --baseline (suppress acknowledged findings), --patches (surgical-fix unified diffs), --fail-on <severity> (CI exit-code gate), --base <ref> (PR-against-base diff scope), --sarif (SARIF v2.1.0 output for GitHub Code Scanning). Ships a reference GitHub Actions workflow and a TAP 13 eval runner over 64 fixtures with positive-and-negative-control coverage. Semantic analysis, not pattern matching. Loosely based on anthropics/claude-code-security-review.",
      "version": "2.3.0",
      "strict": true
    },
    {
      "name": "stride-ideation",
      "source": {
        "source": "url",
        "url": "https://github.com/cheezy/stride-ideation.git"
      },
      "description": "Turn a fuzzy idea into shipped Stride tasks via two slash commands. /stride-ideation:ideate drives an interactive round-based question loop (up to 4 batched AskUserQuestion per round, mandatory round-3 framing checkpoint, mandatory round-4 premortem in v0.3.0, seven hard-gated requirements sections with shape requirements on Assumptions — ranked highest-to-lowest risk with the riskiest marked — and Success Metrics — both leading and lagging indicators required — advisory requirements-reviewer pass) and writes a committed requirements markdown doc — the terminal state, never auto-funnels into the next step. New in v0.4.0: a --profile <lean|product|discovery> flag selects the round structure and reviewer rubric (lean = default, byte-for-byte v0.3.0; product = JTBD four-forces in Round 1 + optional Concrete Example section; discovery = Why-now + Alternative-options in Round 2). New in v0.5.0: a fourth --profile=lean-startup value adds a mandatory Round-5 MVP-design batch anchored on the (R)-marked Assumptions entry, an optional MVP / Validation experiment section in the doc, and two profile-aware advisory reviewer checks (MVP section presence, falsifiable success/failure criteria) — source techniques: Eric Ries (Lean Startup), Steve Blank (Customer Development), Giff Constable (Riskiest Assumption Test). /stride-ideation:stridify (new in v0.2.0; merged from the previous /decompose + /ship pair) takes the requirements doc and runs the full pipeline end-to-end: validates the seven sections, preflights auth from .stride_auth.md, dispatches the requirements-decomposer subagent (canonical batch shape embedded; two-reason multi-goal split rule of code-coupling plus ~10-task soft cap; cross-goal claim ordering documented in decomposition_notes), stamps source_spec + source_spec_sha256, writes and commits a sibling timestamped batch JSON for audit, then strips local-audit fields and POSTs to /api/tasks/batch with token hygiene (no curl -v, no token in logs or errors), rendering the created G/W identifiers. Replaces superpowers:brainstorming for Stride projects; the two skills coexist for non-Stride projects where brainstorming remains the right fit. New in v0.7.0: a four-layer resilience model on /stridify — (1) a bounded subagent-dispatch retry in Step 7 (3 attempts, ~30s/~90s backoff on HTTP 529 / transient network / 'overloaded' classifications; terminal classifications still fail fast), (2) a new --goal <name|index> flag that scopes the decomposer prompt to one surface from the doc's ## Decomposition seams section (accepts integer indices AND slug-matching strings; both --goal value and --goal=value parse forms; the seams section remains freeform and ungated), (3) a retry-exhaustion fallback (new Step 7.5) that writes the assembled prompt + verbatim last error + recovery README to a sibling *-decomposer-prompt.md file when 3 transient failures pile up (Stride API POST is NOT attempted in this branch), and (4) a one-line preflight advisory in Step 2 when a doc enumerates more than 3 surfaces under ## Decomposition seams and --goal is not set. The Stride API POST itself is still NOT retried — partial-batch idempotency is not guaranteed; the user remains the retry mechanism on a 4xx/5xx. Test coverage grows from 59 to 109 assertions (3 new suites: test-stridify-retry, test-stridify-per-goal, test-stridify-fallback). Back-compat is preserved on the happy path — v0.6.0 users see byte-identical behavior unless they invoke --goal or hit a transient subagent failure. New in v0.8.0: /ideate becomes a guided, recoverable, human-in-control session — (1) a display-only per-round completeness recap (the seven gated sections shown solid/thin/empty before every round; never changes the gate, round order, or <=4-question budget), (2) an 'I'm not sure — propose candidates' uncertainty path on every gated-section and forcing question (proposes 2-4 topic-tailored candidates with rationales; a candidate never satisfies the gate until the human confirms it), (3) a profile recommendation before the rounds when --profile is omitted (recommended-first, lean default; explicit --profile skips it; unknown-value fast-fail unchanged), (4) a --input <path> brain-dump seed read read-only to pre-fill draft sections (distinct from and composable with --continue; the file is never modified, moved, or committed), (5) intra-session draft autosave/resume via a new pure-shell lib/draft.sh that persists the in-progress draft after every round to a gitignored .stride/ scratch file and offers resume-or-fresh for the same slug on start, deleting the scratch after a successful commit (never holds the token), and (6) advisory requirements-reviewer findings surfaced as a multi-select human decision with an explicit 'Address none — write as-is' choice (at most one refinement round; the reviewer never blocks the write). /stridify gains a preview-and-approval gate before the POST (renders the decomposed goal/task tree and cross-goal claim order, requires explicit approval; decline stops cleanly with the committed batch JSON intact and no POST) plus a --yes / --auto-approve bypass that preserves the historical fire-and-forget behavior byte-for-byte for scripted callers. Test coverage grows from 109 to 155 assertions across 11 suites (3 new suites: test-stridify-preview, test-ideate-input, test-draft). Additive and back-compat on the happy path; the one automation-affecting change is that a non-interactive /stridify now pauses for approval unless --yes is passed.",
      "version": "0.8.0",
      "strict": true
    },
    {
      "name": "stride-lite",
      "source": {
        "source": "url",
        "url": "https://github.com/cheezy/stride-lite.git"
      },
      "description": "Lightweight companion plugin to Stride — produces Stride-shaped goal and task markdown documents on disk from a free-text prompt plus an optional requirements directory. No API calls, no kanban setup, no auth files. Just markdown. Useful when you want the Stride field discipline (acceptance criteria, key files, pitfalls, testing strategy, dependencies) without committing to the full Stride server workflow — the documents it produces can be reviewed, edited, or later submitted to a real Stride deployment by hand or by tooling, but this plugin never does so itself. Surface: three slash commands and one workflow skill. /stride-lite:create-goal decomposes a prompt into a goal directory with one goal.md plus 1–8 child taskN.md files; /stride-lite:create-task renders a single task markdown file; /stride-lite:init scaffolds a project-local .stride_lite.md with email plus three hook sections (## before_task, ## after_task, ## after_goal). The stride-lite-workflow skill is the file-based equivalent of the full Stride plugin's stride-workflow — it walks a goal directory through an eight-step task lifecycle (select → before_task → explore → implement → after_task → review → review-loop decision → completion + archive), dispatching the two read-only subagents at the right moments. Three subagents are shipped: stride-lite:create-decomposer (decomposes a free-text prompt into a goal + child tasks YAML, no API calls), stride-lite:task-explorer (read-only enrichment with codebase context — Read / Grep / Glob, no Bash, no WebFetch; appends ## Exploration Report), and stride-lite:task-reviewer (reviews changes against acceptance criteria, pitfalls, patterns, and testing strategy with narrowly-scoped read-only git Bash; appends ## Review Report with embedded reviewer_result JSON, schema 1.1). v0.9.0+: hooks/hooks.json registers Claude Code PreToolUse/PostToolUse handlers that auto-fire the three .stride_lite.md sections at the corresponding workflow intercepts — ## before_task before the task-explorer Agent dispatch (blocking, exit 2 stops the dispatch), ## after_task before the task-reviewer Agent dispatch (blocking, exit 2 stops the dispatch), ## after_goal after the goal.md Edit/Write that appends ## Completion Summary (advisory — PostToolUse cannot roll back the write). Cross-platform from day one: hooks/stride-lite-hook.sh (POSIX bash, pure-bash JSON parsing, no jq dependency) and hooks/stride-lite-hook.ps1 (PowerShell 5.1+, ConvertFrom-Json / ConvertTo-Json, no module installs); the .sh script auto-delegates to .ps1 on native Windows (OSTYPE unset + COMSPEC set). v0.10.0+: stride-lite-workflow Step 8 final-task branch performs a terminal PENDING → IMPLEMENTED archive move — after ## after_goal fires, the goal directory moves from docs/implementation/PENDING/<slug>/ to docs/implementation/IMPLEMENTED/<slug>/ via git mv when tracked (preserves rename history) or plain mv otherwise. Collision suffixing on the target with -2 / -3 / … up to a 1000-iteration safety cap (never overwrites), after-goal-failure no-move guard (a structured failed-status JSON from the harness skips the move so the user can re-trigger), non-/PENDING/ path skip with a stderr warning (custom --output-dir is left where it is), and clean exit on filesystem-mv failure (logged, the workflow does not fail because of an archive-move failure). Single-task files under PENDING/tasks/ are NEVER moved — only goal directories.",
      "version": "0.10.0",
      "strict": true
    }
  ]
}
